# AI Handoff — P03.07 Permission Registration

Status: **activation candidate; implementation locked until P03.06 closure / P03.07 activation carrier merges**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md` + `docs/roadmap/work-packages/P03.07.md`

## Activation precondition

P03.07 becomes the sole active package only after the separate P03.06 completion / P03.07 activation closure carrier:

1. is current with protected `main` after P03.06 implementation merge `13dbe8a393c20cabeb8aac60d073a6c66775efd3`;
2. contains governance/evidence/continuity reconciliation only;
3. stays within the approved P03 closure path budget;
4. passes canonical exact-final-head GitHub-hosted governance including all retained P01/P02/P03.01-P03.06 regressions;
5. has no unresolved review/conversation blocker;
6. merges to protected `main`;
7. protected `main`, `STATE.json`, `STATUS.md`, package sequence and this handoff are re-read and the exact post-closure SHA is recorded.

This handoff creates no P03.07 implementation authority before those steps complete.

## Completed prerequisite — P03.06

P03.06 — Capability Registry is implementation-complete with canonical evidence:

- implementation issue: #106 — completed;
- implementation PR: #107 — merged;
- initial implementation head: `28a6343ad10924869d02d666f7b625121f7ecb7d`;
- final exact implementation head: `c895f44a1383d1c1d9c5fd23c95d7864810353c3`;
- implementation merge: `13dbe8a393c20cabeb8aac60d073a6c66775efd3`;
- canonical Governance run/job: `33181421854 / 98883286556` — **PASS**;
- completion evidence: `docs/roadmap/evidence/P03.06_COMPLETION_2026-08-28.md`;
- retained verifier: `scripts/verify_p03_06.sh`.

Governance #467 / `33180840326 / 98881325283` remains diagnostic **FAIL** evidence for the corrected govet variable-shadow finding. P03.01-P03.05 and all P01/P02 prerequisites remain completed and their regression verifiers remain mandatory.

## Objective

Implement only the governed P03.07 Permission Registration contract: register stable module-declared permission definitions into the existing deny-by-default authorization foundation while preserving `kernel.authorization` as the sole enforcement/policy authority.

Owner: `kernel.modules` with `kernel.authorization` enforcement.

## Authorized scope after closure merge

Only after the closure merges, P03.07 may implement:

- stable permission name/owner/module metadata;
- deterministic declaration namespace rules and collision validation;
- optional capability association as descriptive metadata;
- lifecycle-derived permission availability;
- preservation of policy/role references and required history across non-destructive disable/re-enable;
- fail-closed unknown/unavailable permission behavior;
- auditability of material registration/lifecycle changes where required;
- focused positive/adversarial tests;
- a dedicated `scripts/verify_p03_07.sh` verifier;
- canonical governance wiring after retained P03.06 verification.

## Security and architecture invariants

The following are binding:

- permission registration is declaration metadata, never authorization grant;
- `kernel.authorization` remains deny-by-default enforcement and policy authority;
- no role name, including admin/owner/superuser, creates implicit bypass authority;
- tenant/organization scope comes only from trusted P02 context and existing authorization contracts;
- registration cannot accept raw untrusted tenant/org identifiers as authority shortcuts;
- duplicate/conflicting/reserved/invalid permission definitions fail closed;
- unknown or unavailable module permissions deny rather than allow;
- registration cannot mutate role grants implicitly or widen principal scope;
- disabled/unavailable modules cannot continue authorizing behavior through stale registration;
- non-destructive disable/re-enable preserves required policy/history semantics;
- uninstall/disable cannot silently destroy historical audit/policy evidence;
- optional capability association does not grant capability invocation and does not bypass P03.06 non-authorizing metadata rules;
- no duplicate authorization engine or alternate enforcement path is introduced.

## Explicitly unauthorized

P03.07 does not authorize:

- role editor/admin UI;
- a new authorization engine;
- super-admin or role-name bypass behavior;
- entitlements/licensing product logic;
- business-domain permission catalogs before their governed phases;
- P03.08 UI contribution runtime;
- P03.09 migration ownership/execution;
- P03.10 module health runtime;
- P03.11 trust hooks/phase-exit runtime;
- generic remote RPC/service mesh;
- workflow orchestration;
- automatic package acquisition/marketplace runtime;
- product federation runtime;
- business features;
- P04+ runtime;
- strategic X-program runtime;
- AI/model/agent runtime;
- weakening retained governance/security/regression gates.

## Required implementation protocol

1. Finish the P03.06 closure / P03.07 activation carrier without P03.07 runtime code.
2. Require its exact final head to pass canonical governance.
3. Merge only if current with protected `main` and review/conversation gates permit it.
4. Re-read protected `main`, `docs/roadmap/STATE.json`, `docs/roadmap/STATUS.md`, `docs/roadmap/work-packages/P03_PACKAGE_SEQUENCE.json`, `docs/roadmap/work-packages/P03.07.md`, this handoff and governing authorization/security sources.
5. Record the exact post-closure protected-main SHA.
6. Create a **new separate P03.07 implementation branch** from that exact SHA.
7. Before coding, audit current manifest `permissions` declarations, validated discovery snapshots, P03.04 lifecycle availability, P03.06 capability metadata, existing `kernel.authorization` APIs/policy stores and current role/permission model.
8. Record expected affected/unaffected paths, security/tenant implications, migration/data impact (or explicit N/A), rollback/forward-fix and verification commands.
9. Implement only P03.07 scope; material expansion is a stop/re-plan condition.
10. Add adversarial coverage for duplicate/conflicting/reserved declarations, unknown/unavailable permissions, role-name bypass attempts, cross-tenant/scope misuse, implicit grant mutation, lifecycle disable/re-enable history and capability-association non-granting.
11. Add the dedicated P03.07 verifier without removing or weakening retained P01/P02/P03.01-P03.06 gates.
12. Preserve failed/cancelled/stale diagnostic runs as their actual evidence state.
13. Do not mark P03.07 done or activate P03.08 in the implementation PR.
14. Use a later separate governed closure PR for P03.07 completion / P03.08 activation.

## Evidence expected before P03.07 completion

The later implementation PR must prove at minimum:

- deterministic stable permission registration by owner/module/name;
- duplicate/conflicting/reserved/invalid declaration rejection;
- unknown/unavailable permissions deny rather than allow;
- registration cannot create or widen role/principal grants;
- trusted P02 tenant/org scope and contextual authorization remain binding;
- lifecycle availability is accurate while safe history/reference semantics are retained;
- optional capability association remains non-granting and non-invoking;
- no duplicate authorization engine or hidden super-admin bypass;
- retained P01/P02/P03.01-P03.06 regressions;
- exact-head canonical GitHub-hosted governance PASS.

## Next action

Complete the governance-only P03.06 closure / P03.07 activation carrier under GitHub issue #108. Do not implement P03.07 until that carrier passes exact-final-head governance, merges, and protected-main state is re-read.
