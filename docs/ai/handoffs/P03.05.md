# AI Handoff — P03.05 Module Settings & Feature Flags

Status: **activation candidate; implementation locked until P03.04 closure / P03.05 activation carrier merges**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md` + `docs/roadmap/work-packages/P03.05.md`

## Activation precondition

P03.05 becomes the sole active package only after the separate P03.04 completion / P03.05 activation closure carrier:

1. is current with protected `main` after P03.04 implementation merge `13701e7647c1e084dfe4288d4b27b3ddd75e72c2`;
2. contains only governance/evidence/continuity reconciliation;
3. passes canonical exact-final-head GitHub-hosted governance;
4. has no unresolved review/conversation blocker;
5. merges to protected `main`;
6. protected `main` and `STATE.json` are re-read and the exact post-closure SHA is recorded.

This handoff creates no P03.05 implementation authority before those steps complete.

## Completed prerequisite — P03.04

P03.04 — Module Lifecycle State Machine is complete with canonical evidence:

- implementation PR: #100
- final exact head: `cddb42d4466e7f97a7547c4cf5ea0812c768ff0b`
- implementation merge: `13701e7647c1e084dfe4288d4b27b3ddd75e72c2`
- canonical run/job: `33125377739 / 98702150001` — **PASS**
- completion evidence: `docs/roadmap/evidence/P03.04_COMPLETION_2026-08-28.md`
- retained verifier: `scripts/verify_p03_04.sh`

P03.01-P03.03 and all P01/P02 prerequisites remain completed and their regression verifiers remain mandatory.

## Objective

Implement only the governed P03.05 Module Settings & Feature Flags contract by integrating module-declared setting/flag definitions with the existing `kernel.configuration` authority.

Owner: `kernel.modules` with `kernel.configuration` integration.

## Authorized scope after closure merge

- deterministic registration of manifest-declared setting and feature-flag definitions;
- stable module/owner keys, types, validation and defaults;
- lifecycle-aware availability/read semantics;
- tenant/org scoping only through trusted existing configuration context;
- safe disable/unregister/re-enable behavior that preserves required historical configuration;
- collision/ownership validation;
- dedicated positive/negative tests and P03.05 verifier;
- retained P03.04 lifecycle semantics as prerequisite behavior, not a second configuration owner.

## Security and architecture invariants

- `kernel.configuration` remains authoritative for setting/flag state;
- module settings/flags cannot create authority or bypass `kernel.authorization`;
- tenant/org scope comes only from trusted existing P02/P01 configuration context;
- feature flags cannot grant permissions;
- secret values cannot leak through generic module metadata, diagnostics or health projections;
- disable/re-enable cannot silently destroy configuration required for safe history/recovery;
- collisions fail closed;
- no duplicate configuration subsystem may be introduced.

## Explicitly unauthorized

- P03.06 capability registry;
- P03.07 permission registration;
- P03.08 UI contribution runtime;
- P03.09 migration ownership/execution;
- P03.10 health reporting runtime;
- P03.11 trust hooks/phase-exit runtime;
- business-feature configuration domains;
- entitlement/licensing product logic;
- environment ALM/config-as-code (`XALM-200`);
- P04+ event/data runtime;
- package acquisition/marketplace runtime;
- strategic X-program runtime;
- AI/model/agent runtime;
- weakening retained governance/security/regression gates.

## Required implementation protocol

1. After the activation closure merges, re-read protected `main`, `STATE.json`, `STATUS.md`, P03 package sequence and this handoff.
2. Record the exact post-closure main SHA.
3. Create a **new separate P03.05 implementation branch** from that exact SHA.
4. Re-read `kernel.configuration` contracts and ownership before coding.
5. Implement only P03.05 scope and add adversarial validation/tenant/authorization/lifecycle tests.
6. Add a dedicated P03.05 verifier without removing or weakening retained P01/P02/P03.01-P03.04 gates.
7. Preserve failed/cancelled diagnostic runs as their actual evidence state.
8. Do not mark P03.05 done or activate P03.06 in the implementation PR.
9. Use a later separate governed closure PR for P03.05 completion / P03.06 activation.

## Next action

Finish and merge the P03.04 completion/P03.05 activation closure carrier only after exact-final-head canonical governance PASS. Then rehydrate protected-main state and create the separate P03.05 implementation branch. Until then, stop at governance/continuity work.
