# P04.02 Handoff — Publish/Subscribe Abstraction & Ownership Boundaries

Status: **PREPARED / LOCKED — NOT ACTIVE / NO RUNTIME AUTHORITY**

- Phase: `P04 — Data, Jobs & Event Fabric`
- Candidate next package: `P04.02`
- Owner/domain: `kernel.events`
- Contract: `docs/roadmap/work-packages/P04.02.md`
- Predecessor implementation merge: `P04.01` → `6b1a01c009e9a08d45613a00e9b63c0c272bf020`
- Predecessor final exact head: `3c4ee6e79b76a6f042f3ee77b518943f3d7064e4`
- Predecessor promotion PR: `#147`
- Predecessor promotion Governance: run `33331934481` / #548 — PASS

This handoff is continuity/preparation only. It does not change `docs/roadmap/STATE.json`, does not make P04.02 active, and does not authorize source implementation. Canonical state and live GitHub evidence override this file.

## Preconditions before activation

A separate governance/state/continuity transaction must first:

1. re-read fresh protected `main` after P04.01 implementation merge;
2. verify P04.01 completion evidence against exact PR/head/run/merge facts;
3. reconcile the current contradiction/staleness across `AGENTS.md`, `docs/roadmap/STATE.json`, `docs/roadmap/STATUS.md`, package sequence and `docs/ai/AI_STATE.yaml`;
4. record P04.01 `done` without rewriting historical P01/P02/P03 evidence;
5. explicitly accept `docs/roadmap/work-packages/P04.02.md`;
6. make P04.02 the sole `active` package and keep P04.03-P04.10 locked;
7. keep `business_feature_code_authorized=false` and AI/model/agent runtime unauthorized;
8. pass fresh exact-head Omnexa Governance;
9. merge through the protected integration path and re-read main.

If any step fails or state conflicts, P04.02 remains locked.

## Prepared implementation boundary after activation

Only after the activation transaction merges may a **new isolated implementation branch from exact post-transition main** implement the provider-neutral P04.02 boundary:

- validated P04.01 envelope publish abstraction;
- deterministic bounded subscription/handler registration;
- explicit owner/module and versioned event-contract identities;
- duplicate/conflicting registration rejection;
- tenant/context preservation and fail-closed mismatch handling;
- deterministic safe failures;
- focused positive/adversarial tests;
- retained verifier/governance evidence where required.

Expected path budget is approximately:

- `kernel/internal/events/bus.go` (or equivalent single contract file);
- `kernel/internal/events/bus_test.go`;
- narrowly required `kernel/internal/events` utility/export changes only if proven necessary;
- optional `scripts/verify_p04_02.sh` and package evidence/checkpoint artifacts.

## Explicitly locked

P04.02 must not add or implement:

- Kafka/NATS/RabbitMQ/Redis Streams/vendor SDK selection;
- database migrations;
- durable consumer streams/checkpoints (`P04.03`);
- transactional outbox (`P04.04`);
- inbox/deduplication persistence (`P04.05`);
- retry/backoff or DLQ/quarantine (`P04.06`);
- schema-registry runtime (`P04.07`);
- background-job execution changes (`P04.08`);
- business-domain handlers;
- cross-module private writes;
- business-feature authority;
- AI/model/agent authority.

## Retained semantic laws

- duplicate delivery can occur;
- no global ordering guarantee is assumed;
- publish/transport acceptance is not business completion;
- envelope identity/correlation/causation/tenant/classification metadata remains authoritative and unchanged;
- an event is not an authorization credential;
- later protected consumers must be application-idempotent;
- exactly-once provider claims cannot substitute for application duplicate-safety evidence.

## Start protocol after activation

1. Re-read `AGENTS.md`, canonical `STATE.json`, `STATUS.md`, P04 sequence, P04.02 spec and this handoff.
2. Confirm P04.02 is the sole active package and P04.03+ remain locked.
3. Create a new branch from exact protected-main SHA; do not reuse the closure/preparation branch.
4. Record exact owned paths/path budget before source mutation.
5. Read P04.01 envelope implementation/tests so no second envelope model is invented.
6. Implement contract-first with focused tests before broad governance.
7. Preserve provider neutrality and all tenant/authorization/security laws.
8. Require exact-final-head Omnexa Governance and explicit review provenance.
9. Promote unchanged reviewed head through a fresh governed promotion carrier.
10. Do not auto-activate P04.03 after merge; completion/activation remains a later separate transaction.
