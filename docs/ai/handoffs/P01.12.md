# AI Handoff — P01.12 Developer CLI Baseline

Status: **activation candidate in P01.11 closure transition**

## Authority warning

This handoff is subordinate to `AGENTS.md`, `docs/roadmap/STATE.json`, `docs/governance/AI_EXECUTION_POLICY.md`, accepted ADRs and canonical GitHub evidence. Re-verify protected `main` and the P01.11 closure PR before implementation. If the closure has not merged, P01.12 implementation is not yet authorized.

## Entry evidence

P01.11 implementation is merged and completion-eligible:

- PR `#63`;
- final exact head `1c1ab1f8d5120fb6b1e5908fdb93cffef9275940`;
- canonical run/job `32610902537` / `97123708250` — PASS;
- implementation merge `10c94a638b89d47da05f5481fb2db298a2da6942`;
- P01.01-P01.10 regressions PASS;
- repository Go quality PASS;
- canonical evidence `docs/roadmap/evidence/P01.11_COMPLETION_2026-08-23.md`.

## P01.12 objective

Deliver the first repository-owned Omnexa developer CLI/command surface and the final P01 fresh-install exit proof by composing existing kernel foundations into deterministic local/CI workflows without creating hidden authority or bypassing governed verification.

## Authorized scope after closure merges

- stable repository-owned help/version/verify/build-test/approved-diagnostic command surface;
- deterministic fail-closed verification orchestration mapped to existing quality gates;
- explicit process exit codes and structured-safe output;
- safe composition of P01.02 configuration, P01.04 migration and P01.08 diagnostics;
- no-secret and no-RESTRICTED-output behavior;
- deterministic clean-checkout and canonical CI invocation;
- P01.01-P01.11 regression preservation;
- focused positive/negative tests plus a fail-closed P01.12 verifier;
- P01 fresh-install exit proof covering configuration resolution, kernel build/start, fresh PostgreSQL migration, cache/storage provider contracts, safe logs/telemetry, readiness/diagnostics, jobs/configuration/audit primitive tests, canonical developer verification and required security/supply-chain/build gates.

## Forbidden scope

Do not implement production super-admin authority, P02 tenant/user/role administration, P03 module install/runtime administration, P04+ event/workflow/domain commands, deployment/Kubernetes orchestration, hidden SQL/file mutation, business modules/features or AI/model/agent runtime.

CLI convenience does not create authority. Destructive commands require explicit target/environment semantics. Privileged future operations must authenticate, authorize and audit through governed capabilities rather than hidden CLI bypasses.

## Verification direction

P01.12 is the final P01 package, so its implementation evidence must preserve completed P01.01-P01.11 regressions and repository Go quality while accurately recording applicable G0-G8 evidence for the P01 exit risk profile. Required `FAIL`, `BLOCKED`, `NOT RUN` or `N/A` states must never be relabeled PASS.

The fresh-install exit proof must be reproducible on the canonical GitHub-hosted `ubuntu-24.04` lane without undocumented manual SQL/file/UI steps.

## Next authorized action

If protected `main` confirms P01.12 is active after the P01.11 closure merge, create a separate P01.12 implementation branch/PR, re-read the full P01.12 spec plus developer command/testing/CI/release/security standards, implement only the bounded CLI/P01-exit scope, add focused tests and a fail-closed P01.12 verifier, preserve P01.01-P01.11 regressions and obtain the applicable GitHub-hosted evidence.

Do not automatically activate P02 after implementation. P01.12 completion and P01 phase exit require a separate governed reconciliation with complete evidence.
