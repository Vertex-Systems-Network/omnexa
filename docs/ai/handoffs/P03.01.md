# AI Handoff — P03.01 Module Manifest Schema

Status: **active after canonical activation merge**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Activation condition

P03.01 implementation may begin only after the P03 activation PR merges and protected `main` confirms:

- `current_phase=P03`;
- `current_work_package=P03.01`;
- P03/P03.01 active;
- `kernel_code_authorized=true` bounded to P03.01;
- `business_feature_code_authorized=false`.

This handoff is subordinate continuity and does not create authority by itself.

## Objective

Implement only the canonical machine-readable module manifest schema and validation described in `docs/roadmap/work-packages/P03.01.md`.

## Required boundaries

- owner: `kernel.modules`;
- extend `MODULE_STANDARD.md`; do not create a conflicting manifest model;
- parsing/validation treats manifest/package metadata as untrusted data and does not execute module code;
- stable module ID, version, contract version, owner, dependency and declared contribution/security metadata remain explicit;
- required/optional/platform dependency declaration classes remain distinct;
- no secret values or credentials in manifests;
- declared capabilities/permissions are metadata, not authority;
- no registry/discovery before P03.02;
- no dependency resolver before P03.03;
- no lifecycle state machine before P03.04;
- no settings/capability/permission/UI/migration/health registries before their packages;
- no package trust enforcement, marketplace, System Graph, product federation, performance-intelligence, P04, business-domain or AI/model/agent runtime.

## AI-native compatibility boundary

The accepted P03 alignment for XQ-100, XSG-100, XTRUST-100, XPF-200 and XPERF-100 allows only minimal stable metadata/identity hooks required by the P03.01 specification. It does not authorize strategic runtime or speculative framework implementation.

## Verification direction

Implementation must add focused deterministic positive/negative schema tests, malformed/boundary fixtures, repository Go quality and an explicit P03.01 verifier wired into the canonical GitHub-hosted `ubuntu-24.04` governance lane. P01.01-P01.12 and P02.01-P02.10 regressions remain mandatory.

Required implementation completion evidence later includes:

- exact implementation PR and head SHA;
- canonical GitHub-hosted run/job;
- P03.01 verifier evidence;
- security/negative fixtures;
- no unrelated package/runtime changes;
- separate closure/state transition after the implementation merge.

## Next authorized action

After protected `main` confirms the activation merge, **start a new governed session** from that exact main SHA, create a separate P03.01 implementation branch, and implement only P03.01. Do not implement P03.01 in the activation session and do not auto-advance to P03.02 after implementation.
