# AI Handoff — P02.08 Service Accounts & API Credentials

Status: **activation candidate in P02.07 closure transition**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Entry condition

P02.08 implementation is authorized only after the P02.07 closure transition merges and a later governed session confirms on protected `main`:

- `current_phase=P02`;
- `current_work_package=P02.08`;
- P02.01-P02.07 `done` and P02.08 `active`;
- P02 progress `7 / 10 done`;
- `kernel_code_authorized=true` bounded to P02.08;
- `business_feature_code_authorized=false`.

This handoff never creates authority by itself. The closure session must STOP after post-merge verification and must not implement P02.08.

## Objective

Implement the distinct non-human service-principal and bounded API credential lifecycle described in `docs/roadmap/work-packages/P02.08.md` without representing automation as fake human Users or pulling OAuth developer applications, integrations or business scopes forward.

## Authorized boundary

- owner: `kernel.identity`;
- Service Account as a distinct non-human principal while sharing canonical principal semantics;
- tenant/organization binding and capability/permission scope composition through accepted P02 authorization foundations;
- API credential issue, identify, verify, rotate, revoke and expire lifecycle;
- one-time secret presentation where applicable and non-reversible verifier storage;
- classification-safe credential inventory/audit metadata while raw secrets remain `RESTRICTED`;
- last-used/rotation metadata only where classification-safe and useful;
- positive allowed-scope evidence plus wrong-tenant, wrong-scope, revoked and expired negative tests;
- applicable owner-bounded persistence, migrations and focused identity/security verification.

## Security invariants

- non-human principals are never fake human Users;
- raw API credentials are `RESTRICTED` and never logged, traced, audited or stored reversibly;
- credentials remain least-privilege, rotatable, revocable and tenant/organization bound;
- credential possession never bypasses current P02.05/P02.06 authorization state;
- no generic platform superkey/master-token mechanism;
- P02.01-P02.07 human identity, tenancy, organization, authentication/session, authorization and strong-auth ownership boundaries remain intact.

## Forbidden scope

- OAuth developer applications owned later by `platform.developer`;
- external connector/provider integration (`P16`);
- device identities/POS (`P11`);
- AI agent execution identity behavior (`P19-P20`);
- P02.09 tenant-scoped settings;
- P02.10 phase-exit product behavior;
- business API scopes or business features/UI;
- deployment/Kubernetes authority.

## Acceptance direction

The frozen P02.08 acceptance criteria remain unchanged:

1. service-account lifecycle is distinct from User lifecycle while sharing canonical principal semantics;
2. issued credential authenticates only its service principal and allowed tenant/org scope;
3. wrong tenant/scope, revoked and expired credentials fail closed;
4. rotation invalidates superseded credentials according to documented semantics;
5. raw credentials are absent from persistence/logs/traces/errors/audit after allowed one-time issuance surface;
6. no OAuth developer app/integration/business scope is introduced;
7. migrations, Go quality and prior regressions pass as applicable;
8. applicable G0-G8 evidence runs on canonical GitHub-hosted `ubuntu-24.04`.

## Implementation workflow

1. In a later execution session, re-verify protected `main`, canonical `STATE.json`, open PRs and the current P02.08 spec.
2. Verify P02.08 remains the sole active package and there is no competing P02.08 implementation branch/PR to resume.
3. Re-read ownership/dependency/security/testing/Go standards relevant to `kernel.identity` and accepted P02.05-P02.07 authorization/strong-auth boundaries.
4. Create a fresh implementation branch from the exact protected-main SHA.
5. Implement only P02.08 service-account/API-credential lifecycle; do not pull forward P02.09+, OAuth developer apps, integrations, device/POS, AI agents or business scopes.
6. Add deterministic lifecycle, allowed-scope, wrong-tenant/wrong-scope/revoked/expired, rotation invalidation and raw-secret non-disclosure evidence plus a dedicated verifier if required by repository precedent.
7. Preserve repository Go quality, P01.01-P01.12 and P02.01-P02.07 regressions.
8. Run exact-head canonical GitHub-hosted governance and retain all diagnostic FAIL history.
9. Premerge-audit protected-main freshness, diff, reviews/threads and absence of future/business/platform-developer scope.
10. Merge implementation only after observed acceptance, then use a separate closure/state-transition PR.

## Next authorized action

If protected `main` confirms the P02.07 closure transition, **STOP that execution session**. A later governed execution session may then begin P02.08 only after performing the full required pre-engineering reconciliation from the exact protected-main SHA.
