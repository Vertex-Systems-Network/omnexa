# AI Handoff — P03.03 Dependency Graph Resolver

Status: **activation candidate; becomes active only after the P03.02 closure PR merges**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Activation condition

P03.03 implementation may begin only after the P03.02 closure/state-transition PR passes exact-head canonical GitHub-hosted governance, merges to protected `main`, and protected `main` confirms:

- `current_phase=P03`;
- `current_work_package=P03.03`;
- P03.01-P03.02 are `done` with canonical completion evidence;
- P03.03 is the sole `active` package;
- `kernel_code_authorized=true` bounded to P03.03;
- `business_feature_code_authorized=false`.

This handoff is subordinate continuity and creates no authority by itself.

## Completed prerequisites

P03.01 completion evidence: `docs/roadmap/evidence/P03.01_COMPLETION_2026-08-26.md`.

P03.02 accepted identity:

- implementation PR: #94
- final exact head: `0c46db41b0d724a08ea1a78545b3c2debdd8cd05`
- implementation merge: `2e38969dbbbcfcf4765a114f449dc3fa960061d7`
- canonical run/job: `33022405704 / 98355747775` — PASS
- completion evidence: `docs/roadmap/evidence/P03.02_COMPLETION_2026-08-27.md`
- retained verifier: `scripts/verify_p03_02.sh`

## Objective

Implement only deterministic module dependency validation/resolution as specified by `docs/roadmap/work-packages/P03.03.md`, consuming the validated P03.01 manifest and P03.02 deterministic registry/discovery contracts.

## Required boundaries

- owner: `kernel.modules`;
- required dependency presence/version must fail closed before install/enable eligibility;
- optional dependency absence produces selective degradation metadata, not global failure;
- platform dependency constraints are machine-enforced;
- deterministic topological order must not depend on discovery enumeration order;
- cycles and incompatible required constraints are release-blocking invalid states;
- undeclared, forbidden and private cross-module dependency paths must be rejected/provable by fixtures;
- resolver output is metadata/eligibility information only and creates no capability, permission, tenant or database authority;
- kernel cannot depend on business modules;
- no direct cross-module private imports or private-schema writes;
- no lifecycle mutations, package installation/download, P04 orchestration, full System Graph runtime, trust/advisory scanning or business behavior;
- no P03.04-P03.11 implementation;
- no AI/model/agent runtime.

## Verification direction

The implementation must add deterministic tests for required/optional/platform dependencies, stable ordering, cycles, incompatible constraints, undeclared/forbidden/private dependency paths and selective optional degradation. Add a dedicated `scripts/verify_p03_03.sh` and wire it into the GitHub-hosted `ubuntu-24.04` governance lane. Repository Go quality and every completed P01/P02/P03.01/P03.02 regression remain mandatory.

## Next authorized action

After the closure PR merges, start a **new governed session** from that exact protected-main SHA, re-verify canonical state, create a separate P03.03 implementation branch and implement only P03.03. Do not implement P03.03 in this closure session and do not auto-advance to P03.04 after implementation.
