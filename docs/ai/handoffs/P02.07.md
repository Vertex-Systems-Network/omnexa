# AI Handoff — P02.07 MFA / Passkey-Ready Flows

Status: **activation candidate in P02.06 closure transition**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Entry condition

P02.07 implementation is authorized only after the P02.06 closure transition merges and a later governed session confirms on protected `main`:

- `current_phase=P02`;
- `current_work_package=P02.07`;
- P02.01-P02.06 `done` and P02.07 `active`;
- P02 progress `6 / 10 done`;
- `kernel_code_authorized=true` bounded to P02.07;
- `business_feature_code_authorized=false`.

This handoff never creates authority by itself. The closure session must STOP after post-merge verification and must not implement P02.07.

## Objective

Implement the strong-authentication factor/challenge/recovery architecture described in `docs/roadmap/work-packages/P02.07.md` on the established identity/session/authorization foundations, with passkey-ready semantics and `RESTRICTED` authentication-material handling.

## Authorized boundary

- owner: `kernel.identity`;
- MFA factor enrollment, verification and removal lifecycle;
- passkey/WebAuthn-ready credential/challenge contract consistent with approved platform security requirements;
- approved additional factor semantics where implemented;
- recovery-code lifecycle with one-way/secure handling;
- step-up/strong-auth policy hooks for privileged operations;
- replay/expiry/principal-session binding validation for authentication challenges;
- synthetic fixtures and no-secret telemetry/audit behavior;
- applicable owner-bounded persistence, migrations and focused identity/security verification.

## Security invariants

- factor secrets, recovery codes and authentication-equivalent material are `RESTRICTED`;
- no factor/recovery/challenge secret values in logs, traces, errors or audit payloads;
- expired, replayed and wrong-principal/session challenges fail closed;
- factor removal/security-policy change may trigger session invalidation according to policy;
- strong authentication never replaces or bypasses P02.05/P02.06 authorization;
- P02.01 identity, P02.02 tenancy, P02.03 organization, P02.04 authentication/session and P02.05-P02.06 authorization ownership boundaries remain intact.

## Forbidden scope

- P02.08 service-account/API credential lifecycle;
- P02.09 tenant-scoped settings;
- P02.10 phase-exit product behavior;
- P24 enterprise SSO/SAML/SCIM;
- business portal UI/features;
- custom cryptographic algorithms or private-key management outside approved platform primitives;
- deployment/Kubernetes authority;
- AI/model/agent runtime.

## Acceptance direction

The frozen P02.07 acceptance criteria remain unchanged:

1. enrollment/verification/removal lifecycle is deterministic and policy-bound;
2. expired/replayed/wrong-principal challenges fail closed;
3. recovery material is not reversibly exposed after issuance where policy forbids it;
4. privileged step-up hooks can require stronger authentication without bypassing authorization;
5. secret-scanning/redaction negative tests prove no factor material leaks;
6. no SSO/service-account/business scope is pulled forward;
7. migrations, Go quality and prior regressions pass as applicable;
8. applicable G0-G8 evidence runs on canonical GitHub-hosted `ubuntu-24.04`.

## Implementation workflow

1. In a later execution session, re-verify protected `main`, canonical `STATE.json`, open PRs and the current P02.07 spec.
2. Verify P02.07 remains the sole active package and there is no competing P02.07 implementation branch/PR to resume.
3. Re-read ownership/dependency/security/testing/Go standards relevant to `kernel.identity`, P02.04 session lifecycle, and accepted P02.05-P02.06 authorization boundaries.
4. Create a fresh implementation branch from the exact protected-main SHA.
5. Implement only P02.07 MFA/passkey-ready flows; do not pull forward P02.08+, P24 SSO or business UI.
6. Add deterministic enrollment/verification/removal, expired/replayed/wrong-principal, recovery non-disclosure, step-up-without-authz-bypass and secret-redaction evidence plus a dedicated verifier if required by repository precedent.
7. Preserve repository Go quality, P01.01-P01.12 and P02.01-P02.06 regressions.
8. Run exact-head canonical GitHub-hosted governance and retain all diagnostic FAIL history.
9. Premerge-audit protected-main freshness, diff, reviews/threads and absence of P02.08+/P24/business/AI scope.
10. Merge implementation only after observed acceptance, then use a separate closure/state-transition PR.

## Next authorized action

If protected `main` confirms the P02.06 closure transition, **STOP that execution session**. A later governed execution session may then begin P02.07 only after performing the full required pre-engineering reconciliation from the exact protected-main SHA.
