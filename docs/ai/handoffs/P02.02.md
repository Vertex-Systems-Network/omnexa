# AI Handoff — P02.02 Tenant Lifecycle & Trusted Tenant Context

Status: **activation candidate in P02.01 closure transition**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Entry condition

P02.02 implementation is authorized only after the P02.01 closure transition merges and protected `main` confirms:

- `current_phase=P02`;
- `current_work_package=P02.02`;
- P02.01 `done` and P02.02 `active`;
- P02 progress `1 / 10 done`;
- `kernel_code_authorized=true` bounded to P02.02;
- `business_feature_code_authorized=false`.

This handoff never creates authority by itself.

## Objective

Implement only the primary tenant isolation boundary and trusted tenant execution context described in `docs/roadmap/work-packages/P02.02.md`.

## Authorized boundary

- owner: `kernel.tenancy`;
- authoritative Tenant UUIDv7 identity/lifecycle/state;
- explicit tenant-scoped persistence/query semantics where tenant-owned state applies;
- trusted tenant context derived from governed identity/execution relationships, never request-payload authority;
- minimum tenant relationship/membership primitive needed for later scoped authorization;
- same-tenant positive and cross-tenant negative tests;
- fresh/supported-upgrade migration evidence where persistence changes;
- safe structured errors/output and dedicated P02.02 verifier.

## Security invariants

- client-provided tenant IDs are never authorization authority;
- there is no global-tenant fallback;
- tenant isolation applies independently of data classification;
- no hidden support-superuser/cross-tenant bypass;
- `kernel.tenancy` owns the tenant write model; no cross-domain private-table writes;
- P02.01 User identity is not itself tenant membership or tenant authority;
- cross-tenant privileged behavior requires later explicit capability/policy and audit; it is not invented here.

## Forbidden scope

- P02.03 organization hierarchy;
- P02.04 authentication/session implementation;
- P02.05-P02.06 RBAC/relationship policy beyond the minimum P02.02 tenant relationship primitive;
- P02.07 MFA/passkeys;
- P02.08 service-account/API credential lifecycle;
- P02.09 tenant settings;
- P02.10 phase-exit audit product behavior;
- tenant business data/customer/employee records;
- P03 module runtime, business features, deployment/Kubernetes authority, AI/model/agent runtime.

## Verification direction

Implementation must retain repository Go quality, P01.01-P01.12 regressions and P02.01 regression verification; add focused deterministic unit/race tests, real persistence/migration integration where schema changes, explicit same-tenant/cross-tenant negatives, missing-scope and forged-input failures, and `scripts/verify_p02_02.sh` wired into canonical GitHub-hosted `ubuntu-24.04` governance without weakening prior gates.

## Implementation workflow

1. Re-verify protected `main`, `AGENTS.md`, `STATE.json`, this handoff and P02.02 spec.
2. Verify there is no competing P02.02 implementation PR/branch.
3. Create a fresh implementation branch from the exact protected-main SHA.
4. Implement only `kernel.tenancy` P02.02 scope.
5. Add focused positive/negative tests, migration evidence if applicable and `scripts/verify_p02_02.sh`; preserve all prior regressions.
6. Run exact-head GitHub-hosted governance and retain diagnostic FAIL history.
7. Premerge audit branch freshness, diff, reviews/threads and absence of P02.03+/business scope.
8. Merge implementation with expected-head protection.
9. Use a separate closure/state-transition PR; do not activate or implement P02.03 in the implementation PR.

## Next authorized action

If protected `main` confirms this closure transition, the next governed execution session may implement P02.02 only. The closure session itself must STOP after post-merge verification.
