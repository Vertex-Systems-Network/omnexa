# AI Handoff — P03.02 Registry & Deterministic Discovery

Status: **activation candidate; becomes active only after the closure PR merges**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Activation condition

P03.02 implementation may begin only after the P03.01 closure/state-transition PR passes canonical GitHub-hosted governance, merges to protected `main`, and protected `main` confirms:

- `current_phase=P03`;
- `current_work_package=P03.02`;
- P03.01 `done` with canonical completion evidence;
- P03.02 is the sole `active` package;
- `kernel_code_authorized=true` bounded to P03.02;
- `business_feature_code_authorized=false`.

This handoff is subordinate continuity and creates no authority by itself.

## Completed prerequisite

P03.01 completion evidence is `docs/roadmap/evidence/P03.01_COMPLETION_2026-08-26.md`.

Accepted P03.01 identity:

- implementation PR: #92
- final exact head: `87da3302605c852ae5bf43d473aaa01a9e1aaa74`
- implementation merge: `4229e2a28442bf475afed143bab359a770d48053`
- canonical run/job: `33009396644 / 98311433013` — PASS

## Objective

Implement only the deterministic registry/discovery layer over already validated P03.01 manifests as specified by `docs/roadmap/work-packages/P03.02.md`.

## Required boundaries

- owner: `kernel.modules`;
- registry entries consume validated P03.01 manifest metadata rather than defining a second manifest model;
- discovery sources must be explicit and approved; no arbitrary filesystem/network scanning;
- discovery must not execute module/package code or lifecycle hooks;
- duplicate/conflicting module identity/version must fail closed rather than resolve by enumeration order or last-write-wins;
- lookup/list ordering must be deterministic;
- discovered/available metadata must remain distinct from installed/enabled lifecycle state;
- registry metadata cannot grant permissions or capability authority;
- diagnostics must avoid leaking sensitive source/path metadata;
- no P03.03 dependency graph resolution;
- no P03.04 lifecycle execution/persistence;
- no later settings/capability/permission/UI/migration/health/trust runtime;
- no remote marketplace/catalog download or signature/trust enforcement;
- no P04, business-domain or AI/model/agent runtime.

## Verification direction

The implementation must add deterministic tests for empty sources, enumeration-order stability, duplicate/conflicting identities/versions, malformed/unvalidated manifests, source boundaries and non-execution guarantees. Add a dedicated `scripts/verify_p03_02.sh` and wire it into the GitHub-hosted `ubuntu-24.04` governance lane. Repository Go quality and every completed P01/P02/P03.01 regression remain mandatory.

## Next authorized action

After the closure PR merges, start a **new governed session** from that exact protected-main SHA, re-verify canonical state, create a separate P03.02 implementation branch and implement only P03.02. Do not implement P03.02 in the closure session and do not auto-advance to P03.03 after implementation.
