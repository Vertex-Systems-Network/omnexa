# AI Handoff — P03.04 Module Lifecycle State Machine

Status: **activation candidate; implementation locked until closure PR #99 merges**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md` + `docs/roadmap/work-packages/P03.04.md`

## Activation precondition

P03.04 becomes the sole active package only after the P03.03 closure / P03.04 activation PR #99:

1. is current with protected `main`;
2. passes canonical exact-final-head GitHub-hosted governance;
3. has no unresolved review/conversation blocker;
4. merges to protected `main`;
5. protected `main` and `STATE.json` are re-read and the exact post-merge SHA is recorded.

This handoff creates no implementation authority before those steps complete.

## Completed prerequisite — P03.03

P03.03 — Dependency Graph Resolver is complete with canonical evidence:

- implementation PR: #98
- final exact head: `4dcaca22911fbb81b1d25af316fef146c4a71ff3`
- implementation merge: `774fab8b0350ffb2776517e3f1361f76bc2c68f9`
- canonical run/job: `33112808869 / 98659824107` — PASS
- completion evidence: `docs/roadmap/evidence/P03.03_COMPLETION_2026-08-28.md`
- retained verifier: `scripts/verify_p03_03.sh`

P03.01/P03.02 and all P01/P02 prerequisites remain completed and their regression verifiers remain mandatory.

## Objective

Implement only the explicit, deterministic and fail-closed module lifecycle state machine defined by `docs/roadmap/work-packages/P03.04.md`.

Owner: `kernel.modules`.

## Authorized scope after closure merge

- canonical module lifecycle states and legal transitions;
- deterministic transition validation;
- dependency/reverse-dependency lifecycle preconditions;
- safe non-destructive disable and re-enable semantics;
- suspend/archive/detach/purge distinctions;
- explicit destructive purge boundary with audit requirements;
- idempotent/retriable lifecycle operations where applicable;
- concurrency/race protection around state transitions;
- deterministic partial-failure recovery semantics;
- bounded upgrade lifecycle coordination without implementing later migration execution;
- safe, stable operator diagnostics;
- positive and negative tests for illegal transitions, dependency blockers and recovery;
- dedicated P03.04 verifier and canonical governance wiring when implementation exists.

## Security and architecture invariants

- lifecycle state never grants permission, capability, tenant or database authority;
- dependency eligibility remains governed by P03.03 resolver output and cannot be bypassed by lifecycle calls;
- cross-tenant and private-schema boundaries remain deny-by-default;
- required audit failures cannot silently claim protected destructive success;
- purge must be explicit, separately guarded and distinguishable from reversible disable/archive/detach states;
- module lifecycle cannot directly mutate another module's private schema/state;
- deterministic discovery/registry and manifest contracts remain retained prerequisites;
- no hidden filesystem/network/package acquisition side effects are introduced merely by state transition logic.

## Explicitly unauthorized

- P03.05 settings/feature-flag runtime;
- P03.06 capability registry;
- P03.07 permission registration;
- P03.08 UI contribution runtime;
- P03.09 migration ownership/execution beyond bounded lifecycle coordination;
- P03.10 health reporting runtime;
- P03.11 trust hooks/phase-exit runtime;
- P04+ event/data fabric;
- package installation/download/marketplace runtime;
- business modules/features;
- strategic X-program runtime;
- AI/model/agent runtime;
- weakening retained governance/security/regression gates.

## Required implementation protocol

1. Re-read protected `main`, `STATE.json`, `STATUS.md`, P03 package sequence and this handoff after PR #99 merges.
2. Create a **new separate implementation branch** from the exact post-merge main SHA.
3. Implement only P03.04 scope.
4. Add adversarial transition/dependency/idempotency/concurrency/audit tests.
5. Wire a dedicated verifier into canonical GitHub-hosted governance without removing retained checks.
6. Preserve every failed/cancelled diagnostic run as its actual evidence state.
7. Do not mark P03.04 done or activate P03.05 in the implementation PR.
8. Use a later separate governed closure PR for P03.04 completion / P03.05 activation.

## Next action

Finish and merge closure PR #99 only after exact-final-head canonical governance PASS. Then rehydrate protected-main state and create the separate P03.04 implementation branch. Until then, stop at governance/continuity work.
