# AI Handoff — P02.03 Organization Hierarchy & Scoped Memberships

Status: **activation candidate in P02.02 closure transition**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Entry condition

P02.03 implementation is authorized only after the P02.02 closure transition merges and protected `main` confirms:

- `current_phase=P02`;
- `current_work_package=P02.03`;
- P02.01-P02.02 `done` and P02.03 `active`;
- P02 progress `2 / 10 done`;
- `kernel_code_authorized=true` bounded to P02.03;
- `business_feature_code_authorized=false`.

This handoff never creates authority by itself.

## Objective

Implement only the tenant-contained Organization hierarchy and scoped membership relationships described in `docs/roadmap/work-packages/P02.03.md`.

## Authorized boundary

- owner: `kernel.organization`;
- Organization, Legal Entity, Business Unit, Branch, Team and Location hierarchy semantics required by frozen architecture;
- tenant-bound parent/child validation;
- scoped organization membership relationships;
- deterministic hierarchy traversal/validation with cycle and cross-tenant rejection;
- organization/sub-scope context primitives for later policy evaluation without granting authority;
- classification-safe persistence and fresh/supported-upgrade migration evidence where persistence changes;
- focused same-tenant/cross-tenant, invalid-parent, cycle and membership tests plus a dedicated P02.03 verifier.

## Security invariants

- every organization hierarchy node remains tenant-bound;
- hierarchy relationships never cross tenant boundaries;
- tenant membership alone does not authorize every organization/sub-scope;
- Organization is not business Party Organization;
- no ambiguous `company`/`workspace` synonym replaces canonical Tenant/Organization semantics;
- `kernel.organization` owns this write model;
- P02.02 trusted tenant context remains the enclosing tenant-isolation boundary and cannot be bypassed by organization input.

## Forbidden scope

- business Party/Person/Customer/Supplier models;
- P02.04 authentication/session implementation;
- P02.05-P02.06 role/policy enforcement beyond P02.03 relationship/context primitives;
- P02.07 MFA/passkeys;
- P02.08 service-account/API credential lifecycle;
- P02.09 tenant-scoped settings;
- P02.10 phase-exit audit product behavior;
- HR employment, warehouse/store business behavior, CRM or other business modules;
- P03 module runtime, business UI/features, deployment/Kubernetes authority or AI/model/agent runtime.

## Verification direction

Implementation must retain repository Go quality, P01.01-P01.12 regressions and P02.01-P02.02 regressions; add deterministic unit/race tests, real PostgreSQL migration/integration evidence if schema changes, same-tenant hierarchy positives, cross-tenant parent/membership negatives, cycle rejection and missing/invalid scope failures; and wire `scripts/verify_p02_03.sh` into canonical GitHub-hosted `ubuntu-24.04` governance without weakening prior gates.

## Implementation workflow

1. Re-verify protected `main`, `AGENTS.md`, `STATE.json`, this handoff and P02.03 spec.
2. Verify there is no competing P02.03 implementation PR/branch.
3. Create a fresh implementation branch from the exact protected-main SHA.
4. Implement only `kernel.organization` P02.03 scope.
5. Add focused positive/negative tests, migration evidence if applicable and `scripts/verify_p02_03.sh`; preserve all prior regressions.
6. Run exact-head GitHub-hosted governance and retain any diagnostic FAIL history.
7. Premerge audit branch freshness, diff, reviews/threads and absence of P02.04+/business scope.
8. Merge implementation with expected-head protection.
9. Use a separate closure/state-transition PR; do not activate or implement P02.04 in the implementation PR.

## Next authorized action

If protected `main` confirms this closure transition, the next governed execution session may implement P02.03 only. The closure session itself must STOP after post-merge verification.
