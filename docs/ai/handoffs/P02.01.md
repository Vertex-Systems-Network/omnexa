# AI Handoff — P02.01 Principal & User Identity Foundation

Status: **active after governed activation merge**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Entry condition

P02.01 implementation is authorized only after the P01 -> P02 activation transition merges and protected `main` confirms:

- `current_phase=P02`;
- `current_work_package=P02.01`;
- P02/P02.01 active;
- `kernel_code_authorized=true` bounded to P02.01;
- `business_feature_code_authorized=false`.

This handoff never creates authority by itself.

## Objective

Implement only the canonical human principal/User identity foundation described in `docs/roadmap/work-packages/P02.01.md`.

## Required boundaries

- owner: `kernel.identity`;
- User is authentication identity, not business Person;
- non-human principals must not be modeled as fake human users;
- identity attributes do not imply authorization;
- no tenant lifecycle/membership authority before P02.02;
- no organization hierarchy before P02.03;
- no authentication credentials/sessions before P02.04;
- no RBAC/policy before P02.05-P02.06;
- no MFA/passkeys before P02.07;
- no service-account/API credential lifecycle before P02.08;
- no tenant-scoped settings before P02.09;
- no P03/business/deployment/AI runtime.

## Verification direction

Implementation must add focused deterministic positive/negative tests, schema migration evidence if persistence changes, repository Go quality and an explicit P02.01 verifier wired into the canonical GitHub-hosted `ubuntu-24.04` governance lane. P01.01-P01.12 regressions remain mandatory.

Material identity fields must follow the data-classification standard. Do not introduce authentication secrets in this package. Safe errors/logs must not disclose CONFIDENTIAL/RESTRICTED data.

## Implementation workflow

1. Re-verify protected `main`, `AGENTS.md`, `STATE.json`, this handoff and P02.01 spec.
2. Create a fresh implementation branch from that exact main SHA.
3. Implement only `kernel.identity` P02.01 scope.
4. Add focused tests and `scripts/verify_p02_01.sh`; wire it into canonical governance without weakening P01 regressions.
5. Run exact-head GitHub-hosted governance and retain any diagnostic FAIL history.
6. Premerge audit branch freshness, diff, reviews/threads and absence of later P02/business scope.
7. Merge implementation with expected-head protection.
8. Use a separate closure/state-transition PR; do not auto-activate or implement P02.02 in the implementation PR.

## Next authorized action

After protected `main` confirms this activation, the next governed execution session may implement P02.01 only. The activation session itself must STOP after post-merge verification.
