# AI Handoff — P02.10 Identity / Permission Audit Trails & P02 Exit Proof

Status: **activation candidate in P02.09 closure transition**  
Canonical authority: `AGENTS.md` + `docs/roadmap/STATE.json` + `docs/governance/AI_EXECUTION_POLICY.md`

## Entry condition

P02.10 implementation is authorized only after this P02.09 closure transition merges and a later governed session confirms on protected `main`:

- `current_phase=P02`;
- `current_work_package=P02.10`;
- P02.01-P02.09 `done` and P02.10 `active`;
- P02 progress `9 / 10 done`;
- `kernel_code_authorized=true` bounded to P02.10;
- `business_feature_code_authorized=false`;
- P02 exit remains **NOT SATISFIED**.

This handoff never creates authority by itself. The closure session must STOP after post-merge verification and must not implement P02.10.

## Objective

Complete P02 by integrating security-significant identity/tenancy/organization/authorization/configuration activity with the accepted audit foundation and proving the full P02 isolation/permission/session exit contract on canonical CI.

## Authorized boundary

- owner: `kernel.audit` with `kernel.identity`, `kernel.tenancy`, `kernel.organization`, `kernel.authorization`, `kernel.configuration` producers;
- attributable classification-safe audit evidence for material identity/session/tenant/org/role/policy/service-account/settings security operations;
- explicit required-audit fail-closed behavior for privileged operations where correctness requires audit;
- aggregate P02 verification composed from completed package verifiers rather than duplicating them;
- final cross-tenant isolation, object/scope permission, role-difference, service-account and session-invalidation evidence;
- fresh + supported-upgrade migration proof for the P02 schema baseline;
- repository Go quality, P01.01-P01.12 and P02.01-P02.09 regression preservation;
- applicable G0-G8 evidence on canonical GitHub-hosted `ubuntu-24.04`.

## Security invariants

- audit is separate from ordinary logs and stores no credentials/authentication factors/secrets;
- audit write does not imply audit read/export authority;
- privileged security mutations cannot silently claim success when required audit fails;
- cross-tenant and privilege-escalation failures are release blockers;
- P02.01-P02.09 ownership/security boundaries remain binding;
- no P03 activation is implied by P02.10 implementation acceptance.

## Forbidden scope

- generic audit read/export/admin UI;
- support impersonation product surface;
- P03 module runtime/permission registration;
- business domains or business-feature audit catalogs;
- P04 events/workflows;
- AI/model/agent behavior;
- automatic P03 activation or P02 exit without separate closure evidence.

## Acceptance direction

The frozen P02.10 acceptance criteria remain unchanged:

1. material identity/session/tenant/org/role/policy/service-account/settings operations emit attributable classification-safe audit evidence where required;
2. required-audit failure behavior is explicit and fail-closed for protected mutations where specified;
3. canonical tests prove same-tenant success and cross-tenant denial;
4. canonical tests prove object/org/scope permission allow/deny and role differences;
5. canonical tests prove service-account scope, revocation/expiry and no-secret behavior;
6. canonical tests prove required session invalidation/revocation behavior;
7. fresh and supported-upgrade P02 migrations pass without manual database edits;
8. repository Go quality, P01.01-P01.12 and P02.01-P02.09 regressions pass;
9. applicable G0-G8 evidence passes on canonical GitHub-hosted `ubuntu-24.04`;
10. completion evidence is recorded before P02 is reconciled done; P03 remains planned until a separate transition.

## Implementation workflow

1. In a later execution session, re-verify protected `main`, canonical `STATE.json`, open PRs and the P02.10 spec.
2. Verify P02.10 remains the sole active package and there is no competing implementation branch/PR to resume.
3. Re-read `kernel.audit` ownership/dependency/security/testing standards plus all accepted P02 producer boundaries and P02 exit gate.
4. Create or resume only the authorized P02.10 implementation branch from the exact protected-main SHA.
5. Implement only P02.10 audit/aggregate-exit proof; do not pull P03, business-domain, generic audit UI/export, impersonation, workflow/event or AI scope forward.
6. Add deterministic attributable-audit, required-audit-failure, cross-tenant/object-scope/role/service-account/session and migration evidence.
7. Preserve repository Go quality, P01.01-P01.12 and P02.01-P02.09 regressions.
8. Run exact-head canonical GitHub-hosted governance and retain diagnostic FAIL history.
9. Premerge-audit protected-main freshness, diff, reviews/threads and future-scope absence.
10. Merge implementation only after observed acceptance, then use a separate P02 exit-closure/state-transition PR. P03 does not activate implicitly.

## Next authorized action

If protected `main` confirms this P02.09 closure/P02.10 activation transition, **STOP this execution session**. A later governed execution session may then begin P02.10 only after full pre-engineering reconciliation from the exact protected-main SHA.
