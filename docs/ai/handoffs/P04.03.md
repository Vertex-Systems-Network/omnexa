# P04.03 Handoff — Durable Stream/Consumer Baseline & Checkpoint Model

Status: **PREPARED / LOCKED — NOT ACTIVE / NO RUNTIME AUTHORITY**

- Phase: `P04 — Data, Jobs & Event Fabric`
- Candidate next package: `P04.03`
- Owner/domain: `kernel.events`
- Contract: `docs/roadmap/work-packages/P04.03.md`
- Predecessor accepted implementation: P04.02 promotion PR `#156`, exact head `5ec1de746eebc8734f86ec3aa2f311daae0dc18a`, merge `1b378a2f44c6e3cba87b936e39e05f1a18da94cc`
- Predecessor completion evidence: `docs/roadmap/evidence/P04.02_COMPLETION_2026-08-31.md`
- Completion-evidence carrier: PR `#157`, merge/read-back `8334bed24e793e200540bf953f7249f11242badf`

This handoff is continuity/preparation only. It does not change `docs/roadmap/STATE.json`, does not make P04.03 active, does not authorize durable stream/checkpoint source implementation, and does not grant business/AI authority. Canonical protected-main state and live GitHub evidence override this file.

## Preconditions before activation

A separate governance/state/continuity transaction must first:

1. re-read fresh protected `main` at or after `8334bed24e793e200540bf953f7249f11242badf`;
2. verify P04.02 implementation/completion evidence against exact PR/head/run/merge facts;
3. verify this prepared P04.03 contract has itself passed the governed preparation/promotion path;
4. reconcile `AGENTS.md`, `docs/roadmap/STATE.json`, `docs/roadmap/STATUS.md`, package sequence and `docs/ai/` continuity without contradiction;
5. record P04.02 `done` with its exact completion evidence;
6. explicitly accept `docs/roadmap/work-packages/P04.03.md`;
7. make P04.03 the sole `active` package while keeping P04.04-P04.10 locked;
8. keep `business_feature_code_authorized=false` and AI/model/agent runtime unauthorized;
9. record the exact activation base and abort on stale/materially changed main;
10. pass fresh exact-head Omnexa Governance on the transition carrier, use the governed promotion path, merge with expected-head guard, and re-read protected main.

If any prerequisite fails or canonical state conflicts, P04.03 remains locked.

## Prepared implementation boundary after activation

Only after that separate activation transaction merges may a **new isolated implementation branch from exact post-transition protected main** implement the bounded provider-neutral P04.03 contract:

- stable durable consumer identity compatible with P04.02 owner/consumer identity;
- explicit durable stream/partition-equivalent checkpoint scope;
- monotonic checkpoint advancement within one scope;
- deterministic stale/regressive/conflicting checkpoint rejection;
- restart/resume from the last accepted checkpoint;
- fail-closed owner/tenant/scope collision handling;
- explicit at-least-once-compatible duplicate-delivery behavior around crash/restart;
- focused positive/adversarial tests for restart, cancellation, monotonicity, duplicates and tenant/scope isolation.

The implementation must preserve P04.01 envelope metadata and P04.02 ownership laws and must not claim downstream/business exactly-once completion.

## Explicitly locked

P04.03 preparation/implementation must not silently add or authorize:

- Kafka/NATS/RabbitMQ/Redis Streams or another provider/broker selection;
- transactional outbox (`P04.04`);
- inbox/deduplication persistence (`P04.05`);
- retry/backoff or DLQ/quarantine (`P04.06`);
- schema-registry runtime (`P04.07`);
- background-job execution changes (`P04.08`);
- business-domain handlers or cross-module private writes;
- business-feature authority;
- AI/model/agent runtime authority;
- any end-to-end exactly-once claim.

## Retained semantic laws

- duplicate delivery can occur, especially around crash/restart boundaries;
- no global ordering guarantee is assumed;
- checkpoint state represents consumption progress, not authorization or business success;
- event identity/correlation/causation/tenant/classification metadata remains authoritative and unchanged;
- trusted tenant context is validated fail-closed;
- an event, checkpoint token, transport offset or consumer ID is not an authorization credential;
- protected consumers remain application-idempotent;
- provider exactly-once claims cannot substitute for duplicate-safe application evidence.

## Start protocol after activation

1. Re-read `AGENTS.md`, canonical `STATE.json`, `STATUS.md`, P04 sequence, P04.03 spec and this handoff.
2. Confirm P04.03 is the sole active package and P04.04+ remain locked.
3. Create a new implementation branch from the exact protected-main SHA; never reuse this preparation/activation branch.
4. Record an exact owned-path/path-budget decision before runtime mutation.
5. Re-read P04.01 envelope and P04.02 bus/ownership implementations/tests before designing durability so no competing identity or registration model is invented.
6. Implement focused durability/checkpoint behavior first; do not smuggle outbox/inbox/retry/provider semantics into P04.03.
7. Run focused crash/restart, monotonicity, duplicate, cancellation, tenant/scope and malformed-state tests.
8. Require exact-final-head Omnexa Governance and record review provenance honestly.
9. Promote the unchanged reviewed head through the repository's fresh promotion-specific carrier.
10. Do not auto-activate P04.04 after merge; completion/next-package activation remains a later separate governed transaction.
