# P04.06 Completion Evidence — Retry/Backoff, Terminal Failure & Dead-Letter/Quarantine Policy

Status: **IMPLEMENTATION ACCEPTED THROUGH WAVE 2D WORKERS / SUPERVISOR VERIFIER-EVIDENCE CARRIER PENDING / PACKAGE CLOSURE PENDING**

This evidence records the bounded P04.06 implementation and the final two fresh-main closure-gap workers. It does **not** mark P04.06 canonically DONE, activate P04.07, select a broker/provider DLQ, authorize a business feature, claim exactly-once/global ordering, or authorize AI/model/agent product runtime.

## Identity

- Phase: `P04 — Data, Jobs & Event Fabric`
- Package: `P04.06 — Retry/Backoff, Terminal Failure & Dead-Letter/Quarantine Policy`
- Owner/domain: `kernel.events`
- Canonical package state while this evidence is prepared: `ACTIVE`
- Coordination issue for Wave 2D: `#248`
- External provider/spend: `none`
- Business-feature code authority: `none`
- P04.07-P04.10: `locked`

## Accepted implementation boundary

P04.06 now has accepted implementation evidence for:

- deterministic structured failure disposition with unknown-error fail-closed behavior;
- finite attempt/backoff policy, UTC eligibility and hard ceilings;
- durable scheduled/quarantined/resolved state;
- immutable `kernel.events` migration 3 for bounded retry/quarantine persistence;
- PostgreSQL exact-identity create/load, revision CAS transition, due discovery and bounded claim lease;
- authoritative execution gating through a durable claim rather than caller wall-clock time;
- interruption releasing claims without consuming attempt budget;
- terminal/exhausted quarantine before checkpoint advancement;
- quarantine/checkpoint crash-gap recovery without handler replay;
- tenant/owner/consumer/route/stream/partition isolation;
- P04.05 `InboxApplied` / `InboxAlreadyApplied` composition into claimed retry resolution without re-running the protected mutation;
- real-PostgreSQL fresh-pool/store restart preservation for scheduled/quarantined/resolved state.

Checkpoint, inbox completion and retry/quarantine remain separate facts. No retry/quarantine record is authorization, business success, inbox completion or exactly-once proof.

## Accepted evidence chain

### Wave 2A — durable retry/quarantine state

- source PR #227 / promotion PR #228;
- protected merge/read-back `c0311bd79b4d60e9a71fcf0934d9a58f561e1c88`;
- accepted immutable migration: `kernel/migrations/kernel.events/3_create_retry_quarantine_state.sql`.

### Wave 2B — PostgreSQL retry-state CAS/lease persistence

- source PR #230 / promotion PR #231;
- protected merge/read-back `3e6bccf8164a105f92c0b576a99f20a498ec9025`.

### Wave 2C — execution, due discovery, checkpoint composition and verifier

- T01 source #238 / promotion #239 -> `920048584ba039d7c29bd25398c11a2138b2c0b3`;
- T02 source #240 / promotion #241 -> `75cc0f0f590a049639ef0ea5c19a56b22ffab7d1`;
- T03 source #242 / promotion #243 -> `66e1a330c42a37d252e1f32221020d1460aec48f`;
- T04 source #244 / promotion #245 -> `a049164ca3264eff4b71a97dca8e1dbba0c276ae`;
- status/lease closure source #246 / promotion #247 -> `afc1bb6d47cc988e2731bd16da3467d72d55af77`.

### Wave 2D control

- source #249 / unchanged promotion #250;
- control exact head `79cbab0df0dbd1542dd6988fc0ba29290dfd6a7d`;
- source Governance #727 — SUCCESS;
- promotion Governance #728 — SUCCESS;
- protected merge/read-back `43943c91c7491248d3ba1573b72d800da5825f96`.

### Wave 2D T05 — P04.05 already-applied retry resolution

Source #251 / promotion #253, exact head `f1599b6ce92f0e47e0e2b90be5b572097e798f9d`:

- exact paths: `kernel/internal/events/retry_inbox.go`, `kernel/internal/events/retry_inbox_test.go`;
- source Governance #730 / run `34394434256` — SUCCESS;
- promotion Governance #732 — SUCCESS;
- review provenance: Supervisor/SELF REVIEW only; independent approval not claimed;
- zero unresolved review threads at merge decision;
- protected merge/read-back `180d741ef81a1c5e7d33c6bafbc9805d1c801757`.

T05 proves `InboxApplied` and `InboxAlreadyApplied` resolve stale retry scheduling only through an already-authoritative retry claim. The seam has no mutation callback, conflicts/concurrency/unknown states fail closed, and success clears active eligibility/claim/quarantine evidence without rewriting bounded historical failure evidence.

### Wave 2D T06 — real PostgreSQL restart persistence

Final source #254 / promotion #255, exact synchronized head `0c290bc260f63b67c0d0ab75a47fd9a444aa9674`:

- exact path: `kernel/internal/events/retry_state_postgres_restart_integration_test.go`;
- source Governance #735 / run `34396694586` — SUCCESS;
- promotion Governance #736 / run `34397580962` — SUCCESS;
- review provenance: Supervisor/SELF REVIEW only; independent approval not claimed;
- zero unresolved review threads at merge decision;
- protected merge/read-back `df60c3f406dfbc03e4156c60b8103a23ef257e32`.

The prior PR #252 / Governance #734 failure is **diagnostic only**. After T05 moved main, #252 retained the pre-T05 PR event base; M2 correctly saw accepted T05 paths outside T06's one-file lease and failed before Go execution. The branch was synchronized through a real two-parent commit, live compare proved 0-behind/one-file scope, #252 was closed unmerged, and fresh PR #254 with the correct base passed completely. No M2 bypass, validator weakening or production-code change was used.

T06 proves on real PostgreSQL that:

1. scheduled, quarantined and resolved records are persisted through a dedicated writer pool/store;
2. the writer pool is closed completely;
3. a fresh pool/store reopens the same migrated database;
4. exact identity/policy/attempt/revision/failure/eligibility/terminal evidence survives;
5. scheduled state is still `awaiting_claim` after restart;
6. quarantined/resolved states remain non-executable;
7. authoritative bounded due discovery returns the scheduled record;
8. only a fresh successful `ClaimDue` makes it executable.

## Final Supervisor verifier candidate

The T07 Supervisor lease updates only:

- `scripts/verify_p04_06.sh`;
- `docs/ai/handoffs/P04.06.md`;
- this evidence file;
- `README.md`.

The verifier requires the T05/T06 files and named tests in addition to all retained P04.06 markers, migration-3 invariants, real PostgreSQL Retry tests, `go vet`, full event-package regression, Retry race tests and kernel build.

This file cannot truthfully self-record its own future protected merge while it is still part of that candidate. T07 acceptance therefore requires external GitHub evidence: exact-head source Governance, honest review, unchanged promotion Governance, zero unresolved threads, guarded merge and protected-main read-back. After those complete, a separate control/status carrier may close the Wave 2D ledger and synchronize the human README without mutating canonical package state.

## Acceptance criteria reconciliation

The accepted implementation evidence covers the package's required laws:

1. retryable failure schedules bounded later work without checkpoint advancement;
2. terminal/non-retryable failure quarantines without automatic retry;
3. unknown failure fails closed;
4. interruption consumes no attempt and creates no false terminal state;
5. attempts/revisions are monotonic and bounded;
6. backoff/eligibility is deterministic and UTC;
7. pre-eligibility state cannot execute;
8. one authoritative claim gates an eligible attempt;
9. successful completion resolves stale retry state;
10. P04.05 already-applied completion resolves stale retry state without repeated protected mutation;
11. quarantine commits before checkpoint advancement;
12. quarantine persistence failure cannot advance checkpoint;
13. quarantine/checkpoint crash gap recovers without handler replay;
14. owner/consumer/route/stream/partition/tenant isolation fails closed on rebinding;
15. malformed/corrupt retry state fails closed;
16. restart preserves scheduled/quarantined/resolved evidence;
17. post-restart scheduled work still requires authoritative due discovery + claim;
18. due scan and claim bounds prevent unbounded hot-loop authority;
19. durable evidence remains bounded and excludes raw payload/secret/provider diagnostics;
20. no provider-specific DLQ identity becomes canonical policy authority;
21. no P04.07+, business-feature, exactly-once/global-ordering or AI runtime scope leaks into the accepted worker diffs.

## Negative scope retained

P04.06 implementation does not add or authorize:

- P04.07 schema-registry/evolution runtime;
- P04.08 background-job ownership/execution changes;
- P04.09 broad operator recovery/replay UX;
- P04.10 replay/release/poison-event aggregate completion;
- migration 4;
- Kafka, NATS, RabbitMQ, Redis Streams or other broker/provider selection;
- provider-native DLQ semantics as canonical state;
- exactly-once transport, exactly-once external side effects or global ordering;
- production business handlers/features;
- cross-module private-table writes;
- business-feature code authority;
- AI/model/agent product-runtime authority.

## Package closure boundary

**Implementation acceptance is not canonical package completion.** `docs/roadmap/STATE.json` remains authoritative and P04.06 stays ACTIVE until a separate governed closure/next-package transaction explicitly records otherwise.

Required next sequence after T07 acceptance:

1. re-read protected main;
2. close/reconcile the Wave 2D worker/Supervisor ledger in a separate governed control/status carrier;
3. keep P04.07-P04.10 locked while preparing the P04.06 closure / P04.07 activation transaction;
4. require exact-head Governance, review, unchanged promotion, protected merge and read-back for that transition;
5. only then begin P04.07 implementation.
