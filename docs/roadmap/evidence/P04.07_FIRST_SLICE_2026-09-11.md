# P04.07 First-Slice Evidence — Schema Registry, Compatibility & Validation

Status: **T01/T02/T03 ACCEPTED ON PROTECTED MAIN / T04 SUPERVISOR VERIFIER-EVIDENCE CANDIDATE / PACKAGE COMPLETION NOT CLAIMED**

This evidence records the accepted P04.07 Wave 1 first runtime slice and the bounded Supervisor verification candidate. It does **not** mark P04.07 canonically DONE, activate P04.08, reserve migration 4, authorize durable/provider schema-registry persistence, authorize remote references, grant business-feature authority, or authorize AI/model/agent product runtime.

## Identity and authority

- Phase: `P04 — Data, Jobs & Event Fabric`
- Package: `P04.07 — Event Schema Registry, Compatibility & Validation Contract`
- Owner/domain: `kernel.events`
- Canonical state: `docs/roadmap/STATE.json`
- Coordination issue: `#270`
- Implementation plan: `docs/ai/P04.07_IMPLEMENTATION_PLAN.json`
- Worker plan: `docs/ai/ACTIVE_MULTI_AGENT_PLAN.json`
- Native worker capacity used: exactly 3
- Supervisor task: `P04.07-T04` after T01/T02/T03
- Persistence choice for first slice: process-local in-memory registry only
- Migration 4: not reserved / not authorized
- Hosted/provider registry: not authorized
- Remote references: not authorized
- P04.08+: locked
- Business-feature authority: none
- AI/model/agent product-runtime authority: none

The implementation-plan carrier was accepted via source `#271`, promotion `#272`, exact head `f41c20e8f2386de1076e42ccdce1e0f93efe429f`, source Governance run `34529011238`, promotion Governance run `34529016950`, and protected-main read-back `07ab591f37ccf16df74cbadd3cc641c195ebbc43`.

## Accepted worker evidence

### T01 / Agent-01 — local immutable schema registry

- source PR: `#273`
- unchanged promotion PR: `#274`
- exact implementation head: `10dc117b4384c14c287fd68460c6f1cd0e819f02`
- exact files:
  - `kernel/internal/events/schema_registry.go`
  - `kernel/internal/events/schema_registry_test.go`
- source Governance run `34532172982`: SUCCESS
- promotion-context Governance run `34535788308`: SUCCESS
- review provenance: SELF/Supervisor; independent approval not claimed
- protected merge/read-back: `5d61af89743625bd4e39d515a11cd711d1fe01e4`

T01 accepts the provider-neutral typed schema model, bounded normalization, deterministic canonical bytes, lowercase SHA-256 fingerprint, immutable exact identity/content binding, idempotent identical registration, fail-closed conflicting content, defensive registry snapshots and highest accepted lower-version predecessor selection.

### T02 / Agent-02 — deterministic schema compatibility

- source PR: `#275`
- correct unchanged promotion PR: `#278`
- exact implementation head: `181a3bff09d77c39033626077fa6d771a440430e`
- exact files:
  - `kernel/internal/events/schema_compatibility.go`
  - `kernel/internal/events/schema_compatibility_test.go`
- source Governance run `34536975070`: SUCCESS
- promotion-context Governance run `34538151608`: SUCCESS
- review provenance: SELF/Supervisor; independent approval not claimed
- protected merge/read-back: `d06fa216ace09e806ef4dc1c5005cd26d424391e`

The earlier PR `#277` was closed unmerged after Governance correctly rejected its nonconforming registered-agent promotion branch. It is diagnostic evidence only; no validator or branch guard was weakened.

T02 accepts explicit `backward`, `forward`, `full` and `exact` modes, canonical exact comparison, closed-object recursive language containment, homogeneous array recursion, and integer-as-subset-of-number semantics. Compatibility remains evidence only and grants no authorization or protected-mutation authority.

### T03 / Agent-03 — bounded payload validation

- source PR: `#276`
- unchanged promotion PR: `#279`
- exact implementation head: `06d20f7ebd323fa305440c1484045754b2ebf855`
- exact files:
  - `kernel/internal/events/schema_validation.go`
  - `kernel/internal/events/schema_validation_test.go`
- source Governance run `34538968924`: SUCCESS
- promotion-context Governance run `34539864501`: SUCCESS
- review provenance: SELF/Supervisor; independent approval not claimed
- zero unresolved review threads at promotion merge decision
- protected merge/read-back: `e39a9dab525e410dad11f7059e41a1d052b42fd1`

T03 accepts exact local registry lookup plus synchronous bounded JSON validation before protected mutation. Unknown fields, missing required fields, duplicate keys, null, malformed/trailing JSON, wrong types, unregistered identities and limit violations fail closed. Integer tokens remain a strict subset of numbers and no type coercion is introduced.

## Frozen first-slice limits

### Schema

- canonical bytes: `65536`
- nesting depth: `8`
- fields per object: `256`
- aggregate nodes: `2048`
- field-name length: `128` runes
- field names: trimmed nonempty UTF-8; NUL/CR/LF forbidden; duplicates rejected

### Payload

- bytes: `65536`
- nesting depth: `16`
- entries per object/array: `256`
- string length: `4096` runes
- root: closed JSON object
- supported kinds: string, integer, number, boolean, object, homogeneous array
- implicit coercion: none

## Acceptance-law reconciliation

The accepted three-worker slice provides evidence for the bounded implementation-plan laws:

1. payload schema identity uses owner + stable event type + independent positive payload schema version;
2. same exact identity/content is idempotent while same identity/different content fails closed;
3. canonicalization is deterministic and does not mutate caller-owned schema structures;
4. fingerprints are deterministic lowercase SHA-256 over canonical typed content;
5. malformed/unsupported/oversized schema structures fail closed;
6. predecessor selection is the highest accepted lower version for the same owner/event type;
7. exact compatibility requires canonical fingerprint equality;
8. backward and forward directions use the frozen recursive language-containment semantics;
9. full compatibility requires both directions;
10. number accepts the integer subset while integer does not accept general numbers;
11. validation requires a valid closed root object and exact field types;
12. unknown properties, missing required fields and heterogeneous/wrong nested values fail closed;
13. duplicate object keys, null, trailing values, malformed JSON and invalid UTF-8 fail closed;
14. unregistered exact schema identity fails closed before validation success;
15. schema and payload resource limits are explicit and tested;
16. validation errors use stable bounded schema failure classes and do not expose raw payload content;
17. Wave 1 performs no remote reference resolution or external registry I/O;
18. schema validation and compatibility results grant no authorization;
19. schema evidence remains separate from P04.03 checkpoint, P04.05 inbox and P04.06 retry/quarantine state;
20. no migration 4, provider-specific hosted registry, P04.08+, business feature or AI/model/agent product runtime is introduced by the worker diffs.

## T04 Supervisor verifier candidate

The worker plan authorizes T04 only after T01/T02/T03 are accepted. That dependency is now satisfied on protected `main@e39a9dab525e410dad11f7059e41a1d052b42fd1`.

T04 is a Supervisor task, **not a fourth native worker slot**. Its exact write lease is limited to:

- `scripts/verify_p04_07.sh`
- `docs/ai/handoffs/P04.07.md`
- `docs/roadmap/evidence/P04.07_FIRST_SLICE_2026-09-11.md`

The verifier checks:

- governance and P04 activation validators;
- pinned Go toolchain;
- all accepted first-slice source/test files;
- gofmt and Go module/workspace cleanliness;
- migration-4 absence;
- absence of remote/provider/broker/workflow imports from P04.07 schema files;
- frozen registry/compatibility/validation markers;
- named worker acceptance tests;
- focused schema/payload tests;
- `go vet` for `kernel/internal/events`;
- full `kernel/internal/events` regression;
- focused race-enabled schema/payload tests;
- `go build ./kernel/...`.

This file cannot truthfully self-record the T04 carrier's own future Governance/review/promotion/merge/read-back evidence while it is part of that candidate. T04 must still pass source exact-head Governance, honest SELF/Supervisor review without fabricated independent approval, zero unresolved review threads, unchanged promotion-context Governance, protected-main freshness/expected-head guard, guarded merge, and protected-main read-back.

## Retained negative scope

The first slice and this verifier/evidence candidate do not authorize:

- `docs/roadmap/STATE.json` mutation or package completion;
- P04.08 activation or implementation;
- migration 4 or any durable registry table;
- PostgreSQL, Confluent or other hosted/provider registry adoption;
- HTTP(S), DNS, filesystem or registry-to-registry schema/reference fetching;
- dynamic code, eval, plugin, shell or schema callback execution;
- tenant-specific schema forks or caller-selected contract aliases;
- authorization changes or reusable authorization decisions;
- business handlers/features or cross-module private-table writes;
- exactly-once external side effects or global ordering claims;
- AI/model/agent product runtime.

After T04 is accepted, P04.07 remains canonically ACTIVE. Any package completion, durable persistence decision, additional runtime slice, migration reservation or P04.08 transition requires a separate fresh governed transaction.
