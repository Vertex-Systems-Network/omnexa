# P03.07 — Permission Registration Completion Evidence

Status: **implementation accepted; closure/state transition pending**

## Accepted implementation evidence

- implementation issue: #110 — completed
- implementation PR: #111 — merged
- final exact implementation head: `28e36b3ac3183f28ec500f1e70b1fefe02c0c325`
- implementation merge: `66f8b4cc630f6cd865e440a62478df365e042a31`
- canonical Governance run: `33195104185` (#477)
- canonical Governance job: `98930123416`
- result: **PASS**
- runner: GitHub-hosted `ubuntu-24.04`, Linux/X64
- Go: `1.26.7`
- retained verifier: `scripts/verify_p03_07.sh`

The final exact implementation head passed repository Go quality, all retained P01/P02/P03.01-P03.06 regressions, and the dedicated P03.07 permission-registration verifier.

## Implemented contract

P03.07 now provides the governed module-permission registration boundary while preserving `kernel.authorization` as the sole deny-by-default enforcement authority:

- validated manifest snapshots retain module permission declarations;
- stable permission identity is bound to module and owner metadata;
- invalid/reserved/colliding/ownership-conflicting declarations fail closed;
- optional capability association is descriptive only and creates no invocation authority;
- lifecycle-enabled state is required for module-permission availability;
- unknown/unavailable module permissions deny before existing exact-scope RBAC evaluation;
- human and service-account authorization continue through existing authorization contracts;
- module registration creates no role/principal grant and role names create no bypass;
- additive authorization catalog metadata preserves permission identity and role/history references across disable/re-enable;
- no P03.08+ or business-feature runtime was introduced.

## Data / migration evidence

`kernel/migrations/kernel.authorization/4_add_module_permission_registration.sql` is additive. Existing kernel permission rows remain available. Module permission identity/history is retained instead of deleted when unavailable; role/assignment history is not rewritten by registration.

Rollback policy is forward-fix. Do not drop referenced permission rows or destroy role/audit history.

## Diagnostic history retained

The following runs are diagnostic evidence only and are not acceptance authority:

- Governance #474 / `33192567020 / 98921494281` — repository Go-quality FAIL due to one gofmt alignment drift and two unused constants; corrected without semantic or gate weakening.
- Governance #476 / `33194438411 / 98927852853` — repository Go quality plus all retained P01/P02/P03.01-P03.06 regressions passed, then the dedicated P03.07 verifier failed because a source-marker assertion expected two valid SQL predicates on one physical line; verifier was made line-safe by asserting both predicates independently without weakening the invariant.

Only Governance #477 / `33195104185 / 98930123416` on exact head `28e36b3ac3183f28ec500f1e70b1fefe02c0c325` is P03.07 implementation acceptance authority.

## Closure boundary

This evidence file alone does not mark P03.07 `done` and does not activate P03.08. That transition requires a separate governance/evidence/continuity-only closure PR from protected main `66f8b4cc630f6cd865e440a62478df365e042a31`, exact-final-head canonical Governance PASS, current-with-main verification, clean review/conversation gates, merge, and protected-main state re-read.
