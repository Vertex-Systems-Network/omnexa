# P04.05 Completion Evidence — Consumer Inbox/Deduplication & Idempotency Primitive

Status: **IMPLEMENTATION ACCEPTED / PACKAGE CLOSURE PENDING**

This evidence records the accepted P04.05 implementation wave and final canonical acceptance verifier. It does **not** mark P04.05 canonically done, activate P04.06, introduce retry/backoff/DLQ policy, select a broker/provider, grant business-feature authority, or grant AI/model/agent product-runtime authority. P04.05 remains the canonical active package until a later separate governed closure/next-package transaction reconciles state and continuity.

## Identity

- Phase: `P04 — Data, Jobs & Event Fabric`
- Package: `P04.05 — Consumer Inbox/Deduplication & Idempotency Primitive`
- Owner/domain: `kernel.events`
- Active implementation wave: `P04.05-WAVE-20260905-01`
- Protected-main implementation acceptance read-back: `0c66a3371dbf2fa942a95b7d0475b06235392474`
- Worker sequence: T01 core -> T02 PostgreSQL persistence/migration -> T03 reliability/concurrency proof -> T04 Supervisor verifier/Governance integration
- External provider/spend: `none`
- Business-feature code authority: `none`
- P04.06+ runtime authority: `none`

## Accepted implementation boundary

The accepted P04.05 implementation closes only the local duplicate-redelivery-to-protected-mutation window on top of accepted P04.01-P04.04 foundations:

- canonical P04.01 EventID/envelope remains the event identity authority;
- processing identity is scoped to EventID plus accepted owner/consumer/event-type/stream/partition/trusted-tenant binding, so EventID is not a global cross-consumer lock;
- a deterministic SHA-256 canonical-envelope fingerprint provides conflicting-content evidence without turning the inbox into a payload archive;
- `ApplyInbox` receives the caller-owned transaction and performs claim -> protected local mutation -> completion through that same transaction boundary;
- the inbox primitive opens and commits no independent transaction;
- exact completed duplicate delivery returns an explicit already-applied outcome and does not re-run the protected mutation;
- conflicting identity/content reuse and route/tenant rebinding fail closed;
- mutation failure leaves completion unwritten and can roll back normally;
- completion/persistence failure forces the owner transaction to fail rather than committing an unprotected mutation;
- owner-scoped PostgreSQL migration `kernel.events` version 2 persists bounded identity/fingerprint/processing-state/completion evidence only;
- restart after local mutation + inbox commit remains duplicate-safe even when the separate P04.03 checkpoint failed to advance;
- two concurrent same-scope attempts cannot both apply the protected mutation;
- the same canonical EventID remains independently processable by different accepted consumer scopes;
- checkpoint progress and inbox completion remain distinct facts;
- the accepted verifier executes focused real-PostgreSQL inbox tests, retained event regressions, `go vet`, race-enabled Inbox tests and kernel build.

An inbox row, EventID, fingerprint, checkpoint, consumer identity, tenant scope, route, or completion marker grants no authorization or cross-module write authority.

## Wave-control acceptance

Wave-control source PR `#202` and unchanged promotion PR `#203` established the repository-enforced three-worker + isolated-Supervisor execution plan before runtime mutation:

- exact reviewed wave head: `85d41bc6a3100e09fc89ec5e584111ca2b43bcf3`;
- source Omnexa Governance `#655` / run `33918286963` — `PASS`;
- promotion Omnexa Governance `#656` / run `33919392633` — `PASS`;
- guarded promotion merge/read-back: `71eaa84c5a86e39bfc90ab451b8641a8c546cf1e`;
- worker writer cap remained `3`; no claim of 10/100/100K concurrent writers was made;
- migration ownership/version 2 was exclusively reserved to T02;
- P04.06+, provider, business and AI/model runtime remained locked.

## Multi-agent implementation and acceptance evidence

### T01 — Provider-neutral inbox core

Source PR `#204`, unchanged promotion PR `#205`, exact final source/promotion head `52ffeb1dab5aae4aa90a9420001590e8153920e2`:

- exact leased paths: `kernel/internal/events/inbox.go`, `kernel/internal/events/inbox_core_test.go`;
- source Omnexa Governance `#659` / run `33921302355` — `PASS`;
- promotion Omnexa Governance `#660` / run `33921945619` — `PASS`;
- source/promotion review provenance: SELF REVIEW only; independent approval not claimed;
- zero unresolved review threads were required at merge decision;
- expected-head guarded promotion merge/read-back: `c63eaa03260e397094d29fa4896f88e08f5538b7`.

T01 established stable consumer-scoped processing identity, canonical fingerprint conflict evidence, explicit first/duplicate/conflict/concurrent outcomes, exact caller-transaction propagation and protected-mutation/completion ordering without PostgreSQL ownership, checkpoint advancement, provider choice, retry/DLQ or P04.06+ behavior.

### T02 — PostgreSQL inbox persistence and migration

Source PR `#206`, unchanged promotion PR `#207`, exact source/promotion head `473973e1caaf1bac304316c6e3dd4d2b50ac43d6`:

- exact leased paths: `kernel/internal/events/inbox_postgres.go`, `kernel/internal/events/inbox_postgres_integration_test.go`, `kernel/migrations/kernel.events/2_create_consumer_inbox.sql`;
- source Omnexa Governance `#662` / run `33922814111` — `PASS`;
- promotion Omnexa Governance `#663` / run `33924158067` — `PASS`;
- source/promotion review provenance: SELF REVIEW only; independent approval not claimed;
- zero unresolved review threads were required at merge decision;
- expected-head guarded promotion merge/read-back: `f1e432d79ba24761186a8cb66a4e7f45970eceeb`.

T02 supplied the owner-scoped PostgreSQL adapter and pre-reserved `kernel.events` migration version 2. Persistence stores processing identity, canonical fingerprint and local completion state only; it stores no event-payload archive, checkpoint, retry counter, backoff, DLQ/quarantine state, broker receipt, authorization grant or business-domain schema.

Real PostgreSQL integration proves same-transaction protected mutation + inbox completion, migration replay/ledger behavior, restart duplicate suppression, same-EventID cross-consumer independence, conflicting canonical content fail-closed handling, mutation rollback without false completion and inbox-persistence failure forcing owner-mutation rollback.

### T03 — Restart, checkpoint-gap and concurrency proof

Source PR `#208`, unchanged promotion PR `#209`, exact final source/promotion head `e791559f8d9530782b153bd34f6c9192f8ae55d7`:

- exact leased paths: `kernel/internal/events/inbox_reliability_test.go`, `kernel/internal/events/inbox_concurrency_test.go`;
- final source Omnexa Governance `#666` / run `33926481384` — `PASS`;
- promotion Omnexa Governance `#667` / run `33927230809` — `PASS`;
- source/promotion review provenance: SELF REVIEW only; independent approval not claimed;
- zero unresolved review threads were required at merge decision;
- expected-head guarded promotion merge/read-back: `77db8a141b5514d94dea40b5b3acb3722771d7a4`.

A prior source candidate produced diagnostic Governance failure because Go's `copyloopvar` analysis rejected one redundant loop-variable copy in the leased concurrency test. The repair removed only that redundant test assignment; runtime implementation, migration and acceptance laws were unchanged. The failed candidate is diagnostic only and is not acceptance evidence.

T03 proves:

- crash/checkpoint-gap after committed protected mutation + inbox completion causes redelivery without repeated mutation;
- checkpoint recovery can then advance independently;
- overlapping same-scope PostgreSQL transactions yield exactly one protected mutation and one duplicate/already-applied result;
- different accepted consumer scopes can process the same EventID independently even under concurrency;
- route and trusted-tenant rebinding fail before the inbox persistence seam.

### T04 — Canonical P04.05 verifier and Governance gate

Supervisor source PR `#210`, unchanged promotion PR `#211`, exact source/promotion head `dd713fe3217a0d092ab3ff31115ac031ae8c0303`:

- exact leased paths: `scripts/verify_p04_05.sh`, `.github/workflows/governance.yml`;
- source Omnexa Governance `#669` / run `33927932705` — `PASS`;
- promotion Omnexa Governance `#670` / run `33985111334` — `PASS`;
- both runs passed the added final `Verify P04.05 consumer inbox and local idempotency primitive` step after all retained P01/P02/P03/P04.03/P04.04 regressions;
- source/promotion review classification: SELF/Supervisor REVIEW only; independent approval not claimed;
- promotion unresolved review threads at merge decision: `0`;
- protected-main base immediately before promotion merge remained `77db8a141b5514d94dea40b5b3acb3722771d7a4`;
- expected-head guarded promotion merge succeeded as `0c66a3371dbf2fa942a95b7d0475b06235392474`;
- fresh protected-main read-back confirmed `main@0c66a3371dbf2fa942a95b7d0475b06235392474`.

T04 adds no inbox runtime implementation or migration. The verifier requires the accepted T01/T02/T03 contracts, migration v2, focused real-PostgreSQL tests, retained event tests, `go vet`, race tests and kernel build, and rejects undeclared broker/provider, retry/DLQ/schema-registry/job-runtime or P04.06+ expansion.

## Acceptance criteria reconciliation

The accepted wave provides evidence for the P04.05 contract requirements:

1. first valid delivery can commit one protected local mutation plus one inbox completion in the same caller transaction;
2. mutation failure leaves no durable completion;
3. inbox completion/persistence failure prevents the protected mutation transaction from succeeding;
4. exact duplicate delivery after commit does not repeat the mutation;
5. duplicate suppression survives process-object restart;
6. checkpoint-gap redelivery after local commit remains duplicate-safe;
7. rollback before commit leaves a later legitimate attempt eligible;
8. the same EventID remains independent across accepted consumer scopes;
9. trusted tenant/route rebinding fails closed;
10. conflicting canonical content for one processing identity fails closed;
11. concurrent same-scope attempts cannot both commit the protected mutation;
12. cancellation/database/concurrency failure does not create false completion evidence;
13. checkpoint and inbox completion remain separate facts;
14. no P04.06 retry/backoff/DLQ/quarantine, P04.07 schema-registry, job-runtime, broker/provider or business-feature semantics were introduced;
15. no end-to-end/transport/external-side-effect exactly-once claim was introduced;
16. exact-head canonical Governance remained green through final source and promotion acceptance.

Migration evidence additionally covers fresh install/replay/ledger, owner-scoped version 2 identity constraints and forward-safe local duplicate evidence.

## Review and failure provenance

Review/evidence provenance is intentionally not upgraded beyond what GitHub recorded:

- worker source and promotion submissions used honest SELF REVIEW / Supervisor review provenance;
- no independent approval is claimed where none materialized;
- zero unresolved review threads and current-main freshness were required at accepted merge decisions;
- exact-head source and promotion Governance were both required for every implementation tranche;
- T03's redundant loop-variable diagnostic failure was repaired inside the original leased test path without weakening runtime or verifier expectations;
- no stale or failed workflow run is treated as acceptance evidence.

## Negative scope retained

P04.05 did not add or authorize:

- P04.06 retry schedules, backoff policy, retry caps, terminal-failure classification or DLQ/quarantine;
- P04.07 schema-registry/evolution runtime;
- P04.08 background-job ownership/execution changes;
- P04.09 broad operator recovery/replay UX;
- P04.10 terminal replay/poison-event package acceptance;
- Kafka, NATS, RabbitMQ, Redis Streams or another external broker/provider selection;
- a distributed PostgreSQL/broker transaction;
- exactly-once transport, exactly-once external side effects or end-to-end exactly-once business processing;
- cross-module private-table write authority;
- production business handlers/features;
- business-feature code authority;
- AI/model/agent product-runtime authority.

## Closure / next-package boundary

This file is implementation completion evidence only. P04.05 remains canonically active until a **separate governed state/continuity transaction** records it done.

Before any P04.06 runtime can become active, the repository must separately:

1. accept this evidence carrier through fresh exact-head Omnexa Governance, honest exact-head review provenance, zero unresolved threads and expected-head guarded merge/read-back;
2. re-read fresh protected main;
3. prepare/review the P04.06 retry/backoff/terminal-failure/DLQ contract if it is still only planned and has no accepted spec;
4. accept that preparation through its own governed source/promotion path while P04.05 remains active;
5. only then execute a separate atomic P04.05-closure/P04.06-activation transaction against fresh protected main;
6. keep provider selection, business-feature authority and AI/model/agent runtime locked unless a later explicit package grants them;
7. require fresh exact-head Governance and protected-main read-back for every separate carrier.

Until those later transactions complete, **do not implement P04.06 runtime source**.
