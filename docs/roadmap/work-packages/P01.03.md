# P01.03 — Structured Error & Result Conventions

- Phase: `P01`
- State: `done`
- Owner/domain: `kernel.platform`
- Depends on: `P01.02 done`
- Blocks: `P01.04`–`P01.12`
- Change class: `B`

> **Completion state:** P01.03 is complete with GitHub-hosted executable evidence. `business_feature_code_authorized=false` remains mandatory. Canonical evidence: `docs/roadmap/evidence/P01.03_COMPLETION_2026-08-22.md`.

## Objective

Implement the frozen P00 error semantics as reusable Go primitives so kernel and later modules can distinguish machine-stable failures from safe public detail without leaking internal causes.

## Delivered scope

- typed/stable machine error codes;
- safe public message/detail separated from private diagnostic cause;
- wrapping/unwrapping without losing classification;
- explicit retryability/failure-category metadata;
- deterministic bounded validation-field errors;
- request/correlation trace attachment boundary without telemetry emission;
- transport-neutral failure primitives suitable for later HTTP/jobs/events adapters;
- standard Go `errors.Is` / `errors.As` semantics;
- canonical Go `(value, error)` result convention retained rather than introducing a custom `Result[T]` abstraction;
- regression compatibility with completed P01.01/P01.02 behavior;
- GitHub-hosted verifier and negative security tests.

## Explicitly out of scope

P01.03 did not implement:

- HTTP Problem Details transport adapter beyond stable hooks;
- PostgreSQL/provider-specific error mapping (`P01.04+`);
- cache/storage errors owned by later packages;
- logging/OpenTelemetry emission (`P01.07`);
- health/readiness endpoints;
- job/scheduler behavior;
- tenant/authz/module-runtime concerns;
- domain/business error catalogs or business APIs.

## Security invariants

Stack traces, SQL, secrets, credentials, private keys, raw provider payloads, local filesystem paths and sensitive tenant/business data do not enter the public error representation. Internal causes remain private to authorized diagnostic consumers introduced by later packages.

Error codes are machine-stable identifiers, not localized prose. Retryability is explicit metadata, never inferred from message text. Correlation identifiers are metadata only and cannot grant authority.

## Acceptance criteria

1. Stable code and safe public message survive wrapping. **PASS**
2. `errors.Is`/`errors.As` semantics remain usable for wrapped causes/classified errors. **PASS**
3. Negative tests prove private cause/secret text cannot reach public representation. **PASS**
4. Validation failures have deterministic bounded field structure and duplicate/ordering rules. **PASS**
5. Retryable/non-retryable semantics are explicit and testable. **PASS**
6. Result/error primitives contain no HTTP, persistence, telemetry or business-domain coupling. **PASS**
7. P01.01 and P01.02 regression verifiers continue to pass. **PASS**
8. Applicable G0/G1/G2/G5/G7 evidence passes on GitHub-hosted `ubuntu-24.04`. **PASS**

## Completion evidence

Canonical record: `docs/roadmap/evidence/P01.03_COMPLETION_2026-08-22.md`.

Key immutable evidence:

- implementation PR #44;
- final implementation head `c9636bf9465c0c17d59e783e3ee73c3ba05fe22c`;
- final hosted run `32565935613` / job `97014452248` — SUCCESS;
- runner `GitHub Actions 1000008114`, Ubuntu 24.04.4 LTS / X64;
- Go `1.26.7`;
- squash merge `bdeda5fad09a2369b2a6852e5c62550db50047ea`.

Failed and diagnostic runs are preserved in the canonical evidence record and are not counted as PASS.

## State transition

`active -> verification -> done` is complete.

**Only P01.04 may be active next.** P01.05+, P02/P03 and business-feature implementation remain unauthorized until their own governed transitions.
