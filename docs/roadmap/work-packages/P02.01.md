# P02.01 — Principal & User Identity Foundation

- Phase: `P02`
- State: `done`
- Owner/domain: `kernel.identity`
- Depends on: `P01 done`, `P02 entry gate satisfied`
- Blocks: `P02.02`–`P02.10`
- Change class: `B`

> P02.01 is complete. `business_feature_code_authorized=false` remains locked. Completion unlocks only P02.02 through the separate governed closure transition.

## Objective

Establish the canonical human identity/principal model and lifecycle required by later tenancy, authentication and authorization packages without creating a business Person model or pulling authentication/session behavior forward.

## In scope

- stable UUIDv7 identity/principal identifiers;
- human `User` identity lifecycle and explicit lifecycle states;
- principal-type vocabulary/extensibility required by the Security Standard;
- classification-safe identity attributes needed before tenancy/authentication;
- repository/persistence boundary owned only by `kernel.identity`;
- deterministic constructors/state transitions and disclosure-safe errors;
- positive/negative tests and fresh/idempotent/immutable migration evidence.

## Out of scope

- business `Person` / Party records;
- tenant lifecycle or membership authority (`P02.02`);
- organization hierarchy (`P02.03`);
- authentication credentials/sessions (`P02.04`);
- roles/policies (`P02.05-P02.06`);
- MFA/passkeys (`P02.07`);
- service-account/API credential lifecycle (`P02.08`);
- business modules, P03 module runtime, deployment or AI/model/agent behavior.

## Security and architecture invariants

- User is authentication identity, never a substitute for business Person.
- Non-human identities are not fake human users.
- Identity attributes never imply authorization by themselves.
- Secrets/authentication material are not introduced by this package.
- Owner is `kernel.identity`; no cross-domain private-table writes.
- Tenant claims are not authority.

## Acceptance criteria

1. Canonical User/principal identity and lifecycle semantics are deterministic and owner-bounded. **PASS**
2. UUIDv7 and canonical error/time conventions are reused rather than duplicated. **PASS**
3. Invalid lifecycle transitions fail closed. **PASS**
4. User/Person separation is explicitly tested/documented. **PASS**
5. No authentication/session/tenant/RBAC/service-account authority is introduced early. **PASS**
6. Schema changes pass fresh/idempotent/immutable-ledger migration evidence. **PASS**
7. Repository Go quality and all P01 regressions remain green. **PASS**
8. Applicable G0-G8 evidence runs on canonical GitHub-hosted `ubuntu-24.04`. **PASS**

## Completion evidence

- implementation PR: `#69`
- final exact implementation head: `76919a9588f70aeea7e00f5214b82dcbf34cbee7`
- implementation merge: `44882e91e49d0364d841b511edbfd0619d05de1f`
- final canonical GitHub-hosted run/job: `32635243643 / 97183883007` — **PASS**
- runner: `GitHub Actions 1000013607`, Ubuntu 24.04.4 LTS / X64, image `ubuntu-24.04 / 20260816.277.1`
- Go: `1.26.7`
- repository Go quality: PASS — gofmt 72 files, golangci-lint v2.12.2 0 issues, govulncheck v1.7.0 no vulnerabilities
- P01.01-P01.12 regressions: PASS
- P02.01 G0-G8: PASS
- immutable evidence: `docs/roadmap/evidence/P02.01_COMPLETION_2026-08-23.md`

Initial canonical run `32635051321 / 97183427697` remains retained as **FAIL** for 13 corrected `govet` shadow findings in the integration test. Source was fixed directly; no linter or governance gate was weakened.

## State transition

`planned -> active -> verification -> done`. Current state is `done`. This completion unlocks only P02.02 through the separate governed closure and does not authorize P02.03+.
