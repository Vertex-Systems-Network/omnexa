# P03.02 — Registry & Deterministic Discovery

- Phase: `P03`
- State: `done`
- Owner/domain: `kernel.modules`
- Depends on: `P03.01`
- Blocks: `P03.03`
- Change class: `B`

> P03.02 is complete with canonical implementation evidence and remains a mandatory regression prerequisite. P03.03 is now the sole active package. `business_feature_code_authorized=false` remains unchanged.

## Objective

Build a deterministic registry/discovery layer over validated P03.01 manifests so available module identities and versions can be enumerated without implicit hook execution, network trust assumptions or hidden filesystem coupling.

## In scope

- registry records for validated module identity/version/owner/source metadata;
- deterministic discovery from explicitly approved repository/runtime sources;
- duplicate/conflicting module ID/version rejection;
- stable lookup/list ordering and source/version identity;
- separation between discovered/available metadata and installed/enabled lifecycle state;
- discovery diagnostics safe for operators and later health integration.

## Out of scope

- remote marketplace/catalog download;
- dependency ordering/compatibility resolution;
- install/enable/disable execution;
- signature/trust decisions;
- federated products, business modules or AI runtime.

## Security and architecture invariants

- discovery parses validated metadata and must not execute module code;
- discovery sources are explicit; arbitrary path/network scanning is forbidden;
- duplicate identity cannot be resolved by nondeterministic last-write-wins behavior;
- registry metadata cannot grant permissions/capability authority;
- package paths/source details exposed in diagnostics must respect classification/redaction.

## Acceptance criteria

1. deterministic discovery returns the same validated registry from the same source set regardless of enumeration order.
2. duplicate/conflicting IDs or versions fail closed with explicit diagnostics.
3. malformed/unvalidated manifests never enter the registry.
4. discovery performs no lifecycle hook execution or unapproved network access.
5. discovered versus installed/enabled state is structurally distinguishable.
6. tests cover empty source, duplicate, conflicting version, malformed source and deterministic-order cases.
7. repository Go quality and P01/P02/P03.01 regressions pass on GitHub-hosted `ubuntu-24.04`.

## Completion evidence

Canonical completion evidence: `docs/roadmap/evidence/P03.02_COMPLETION_2026-08-27.md`.

Accepted implementation identity:

- implementation PR: #94;
- final exact implementation head: `0c46db41b0d724a08ea1a78545b3c2debdd8cd05`;
- protected-main implementation merge: `2e38969dbbbcfcf4765a114f449dc3fa960061d7`;
- canonical GitHub-hosted workflow run/job: `33022405704 / 98355747775` — PASS;
- dedicated verifier: `scripts/verify_p03_02.sh` — PASS inside canonical governance.

All P01/P02/P03.01/P03.02 completed-package regressions remain mandatory until a later governed change explicitly replaces them.

## Forward compatibility — ADR-0012

Accepted ADR-0012 adds a bounded P03.03 prerequisite without changing P03.02's historical public registry contract.

The retained P03.02 invariants are:

- at most one discovered record per module ID;
- duplicate identities fail closed;
- conflicting module versions fail closed;
- public `List`/`Lookup` record/cardinality/order semantics remain deterministic;
- registry records remain metadata only and grant no lifecycle/capability/permission authority;
- discovery remains explicit-source only and performs no filesystem/network scan or module execution.

For the active P03.03 resolver, ADR-0012 permits an additive package-private discovery binding:

- version-specific manifest parsing produces a normalized validated manifest snapshot;
- discovery atomically associates that snapshot with the same unique module ID used by the public registry record;
- the snapshot carries the schema version plus normalized required/optional dependency declarations needed by P03.03;
- resolver dependency declarations must come only from that registry-bound snapshot;
- a second independently reparsed raw-manifest set is not resolver authority;
- missing/mismatched/mutable associations fail closed;
- package-private snapshots must not alter public P03.02 lookup/list/cardinality semantics.

This is forward evidence under P03.03, not a rewrite of P03.02 completion. Historical P03.02 implementation/run/evidence stays immutable and mandatory as a regression baseline.

## State transition

`planned -> active -> verification -> done`; P03.02 remains `done`. P03.03 is the sole active package. P03.04 remains planned and unauthorized until P03.03 separately completes and a later governed closure activates it. Do not auto-advance.
