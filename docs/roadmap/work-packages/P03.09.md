# P03.09 — Migration Ownership Registry

- Phase: `P03`
- State: `done`
- Owner/domain: `kernel.modules` integrated with the P01 database/migration foundation
- Depends on: `P03.08`
- Blocks: `P03.10`
- Change class: `B`

> **Completion accepted.** P03.09 implementation is complete through issue #120 / promotion PR #122. Exact implementation head `8c4da1c1c9e11dfe2f1fa4b81b730140a9f24d56` passed promotion-specific canonical GitHub-hosted Governance #495 / run-job `33223035182 / 99020954655`, and merged as `ea402964c45a630fd6723e0e4a6754555a6a4994`. This completion record does not itself authorize P03.10 before the separate P03.09 closure / P03.10 activation carrier passes exact-final-head governance, merges, and protected-main state is re-read. `business_feature_code_authorized=false` remains binding.

## Objective

Bind ordered/versioned migrations to module ID/version/authoritative owner so fresh install and supported upgrade paths can be proven without allowing cross-module schema mutation or environment-specific manual database changes.

## In scope

- migration identity/order/module/version/owner registry metadata;
- ownership checks against module/domain boundaries;
- fresh-install and supported-upgrade planning/validation;
- destructive/backfill classification metadata and lifecycle coordination;
- duplicate/order/conflict detection;
- representative migration fixtures used by P03 exit proof;
- focused positive/adversarial tests and dedicated `scripts/verify_p03_09.sh` verification;
- canonical governance wiring after retained P03.08 verification.

## Out of scope retained

- replacing the P01 migration engine;
- cross-domain schema ownership changes without ADR;
- marketplace arbitrary SQL execution;
- environment-specific manual SQL as accepted evidence;
- P04 event/outbox migrations beyond their later phase;
- P03.10+ runtime;
- business-feature code;
- strategic X-program runtime;
- AI/model/agent runtime.

## Security and architecture invariants

- a module may migrate only owned schema unless an explicitly approved platform migration says otherwise;
- direct cross-module table mutation is forbidden;
- migration inputs/paths are not arbitrary user-controlled file or SQL execution;
- destructive operations require explicit documented strategy and lifecycle authorization;
- migrations preserve tenant boundaries and data integrity;
- retries/concurrency cannot double-apply a migration;
- P03.09 extends ownership/validation metadata around the retained P01 migration foundation rather than replacing it;
- trusted tenant/organization context remains governed by P02 contracts;
- no hidden cross-module database authority, generic RPC/service-mesh authority or business-feature shortcut is introduced.

## Acceptance criteria

1. migration metadata binds deterministic order to module/version/owner and rejects duplicate/conflicting identities.
2. cross-owner schema mutation fixtures are rejected unless explicitly approved by canonical governance.
3. fresh install and representative supported upgrade paths are mechanically testable without manual SQL.
4. destructive/backfill requirements are explicit and cannot be hidden as ordinary compatible migration.
5. lifecycle upgrade/failure behavior preserves a recoverable/forward-fix state.
6. tenant isolation and existing P01 migration regressions remain intact.
7. retained P01/P02/P03.01-P03.08 regressions passed on GitHub-hosted `ubuntu-24.04`.
8. dedicated P03.09 verification pins deterministic identity/order/owner, conflict rejection, destructive/backfill classification, retry safety and tenant-boundary invariants.
9. no P03.10+, P04+ or business-feature runtime was introduced by P03.09 implementation.

## Completion evidence

- implementation issue: #120 — completed;
- draft implementation carrier: #121 — closed unmerged after exact-head Governance #494 / `33222631307` SUCCESS;
- promotion implementation PR: #122 — merged;
- final exact implementation head: `8c4da1c1c9e11dfe2f1fa4b81b730140a9f24d56`;
- promotion-specific canonical Governance run/job: `33223035182 / 99020954655` — **PASS**;
- implementation merge: `ea402964c45a630fd6723e0e4a6754555a6a4994`;
- immutable completion evidence: `docs/roadmap/evidence/P03.09_COMPLETION_2026-08-29.md`;
- retained verifier: `scripts/verify_p03_09.sh`.

Governance #493 / `33222404123` remains diagnostic **FAIL** evidence for formatter alignment only. #494 is successful exact-head draft-carrier evidence; #495 is the promotion-specific merge authority.

## State transition

`planned -> active -> verification -> done` is complete for P03.09. P03.10 becomes active only through the separate governed closure transition tracked by issue #123; do not auto-advance to P03.11.
