# P01.12 — Developer CLI Baseline

- Phase: `P01`
- State: `done`
- Owner/domain: `kernel.developer`
- Depends on: `P01.11 done`
- Blocks: `P01 exit` — **satisfied**
- Change class: `B`

> **Completion boundary:** P01.12 implementation is complete with canonical GitHub-hosted evidence. `business_feature_code_authorized=false` remains mandatory. Completion does not activate P02 or grant P02/P03/business/deployment/AI authority.

## Objective

Deliver the first repository-owned Omnexa developer CLI/command surface that composes P01 foundations into deterministic local/CI workflows without hiding required steps or granting privileged runtime authority.

## In scope

- stable `omnexa`/repository command surface for version, verify, build/test helpers and approved local diagnostics;
- canonical `verify` orchestration mapping to applicable quality gates;
- explicit exit codes and structured-safe output;
- command help/version metadata;
- configuration integration from P01.02;
- migration command boundary from P01.04 where explicitly safe;
- diagnostic/health command boundary from P01.08;
- no-secret output policy;
- deterministic invocation from clean checkout and CI.

## Out of scope

- production super-admin CLI;
- tenant/user/role administration (`P02`);
- module install/runtime CLI (`P03`);
- event/workflow/domain commands (`P04+`);
- deployment/Kubernetes orchestration;
- interactive commands that bypass auditable APIs.

## Security invariants

CLI convenience never creates authority. Privileged future operations must authenticate/authorize through governed capabilities and audit paths. Command output must redact secrets and RESTRICTED data. Destructive commands require explicit semantics; no hidden SQL/file mutation is permitted.

## Acceptance criteria — verified result

1. Clean checkout exposes deterministic help/version/verify behavior — **PASS**.
2. `verify` returns non-zero when any required applicable gate fails — **PASS**.
3. CLI output contains no secret/local credential leakage — **PASS**.
4. Commands compose existing P01 capabilities rather than reimplementing them — **PASS**.
5. CI invokes the same semantic verification command family used locally — **PASS**.
6. No P02/P03/business/deployment administration is introduced — **PASS**.
7. Applicable G0–G8 evidence for the P01 exit risk profile is recorded on canonical GitHub-hosted `ubuntu-24.04` — **PASS**.
8. P01 fresh-install exit gate proves start, migrate, health-check, telemetry and tests reproducibly without hidden manual steps — **PASS**.

## Completion evidence / P01 exit evidence

Canonical implementation and phase-exit evidence:

- implementation PR: `#65`;
- final exact head: `2ee9a619f3bf828a4c38f8f3af7277fe8c7634f9`;
- implementation merge: `eeebaf5ae3817588b014ddf4c9911bca52c97ed7`;
- final canonical run/job: `32629072886 / 97168916985` — **PASS**;
- runner: `GitHub Actions 1000013010`, GitHub-hosted Ubuntu 24.04.4 LTS / X64;
- runner image: `ubuntu-24.04 / 20260816.277.1`;
- Go: `1.26.7`;
- repository Go quality: PASS, golangci-lint `v2.12.2` 0 issues, govulncheck `v1.7.0` no vulnerabilities;
- P01.01-P01.11 regressions: PASS;
- real `omnexa db migrate`: PASS;
- real `omnexa verify all`: PASS;
- P01.12 G0-G8: PASS;
- package evidence: `docs/roadmap/evidence/P01.12_COMPLETION_2026-08-23.md`;
- phase exit gate: `docs/governance/P01_EXIT_GATE.md` — **SATISFIED**.

Initial run `32628865023 / 97168401358` remains retained as **FAIL** for corrected gofmt/G204/ST1005 diagnostics and is not completion evidence.

## State transition

`active -> verification -> done` is satisfied by the canonical implementation and exit evidence above. P01.12 is `done`; P01 may reconcile `done` at 12/12. P02 remains `planned` and requires a separate governed activation. After P01 completion reconciliation, no new implementation scope is authorized until that later transition.
