# P03.07 — Permission Registration

- Phase: `P03`
- State: `done`
- Owner/domain: `kernel.modules` with `kernel.authorization` enforcement
- Depends on: `P03.06`
- Blocks: `P03.08`
- Change class: `B`

> Completion is supported by canonical exact-head GitHub-hosted evidence. `business_feature_code_authorized=false` remains binding. P03.08 activation belongs only to the separate governed P03.07 closure / P03.08 activation transition; P03.09+ remains locked.

## Objective

Register stable module-declared permission definitions into the existing deny-by-default authorization foundation while preserving `kernel.authorization` as enforcement/policy authority.

## In scope

- stable permission name/owner/module metadata;
- declaration collision/namespace validation;
- optional capability association as descriptive metadata only;
- lifecycle-derived permission availability;
- preservation of policy/role references/history across non-destructive disable/re-enable;
- fail-closed behavior for unknown/unavailable permissions;
- auditability of material registration/lifecycle changes where required;
- focused positive/adversarial tests;
- a dedicated `scripts/verify_p03_07.sh` verifier and retained canonical governance wiring.

## Out of scope

- role editor/admin UI;
- new authorization engine or super-admin bypass;
- entitlements/licensing;
- business-domain permission catalogs before their phases;
- P03.08+ runtime;
- P04+ runtime;
- business-feature code;
- strategic X-program runtime;
- AI/model/agent runtime.

## Security and architecture invariants

- registration is declaration metadata, never authorization grant;
- server-side enforcement remains deny-by-default `kernel.authorization`;
- role names such as admin/owner/superuser never create bypass authority;
- tenant/org scope comes only from trusted P02 context and cannot be supplied as a raw untrusted registry shortcut;
- disabled/unavailable module permissions cannot authorize unavailable behavior;
- uninstall/disable cannot silently destroy historical audit/policy evidence;
- optional capability association is metadata only and cannot convert capability discovery into permission grant;
- permission registration cannot mutate role grants implicitly, widen principal scope or bypass contextual policy;
- P03.06 capability metadata remains non-invoking and non-authorizing.

## Acceptance criteria

1. valid stable permission declarations register with owner/module identity and deterministic namespace rules.
2. duplicate/conflicting/reserved/invalid permission definitions fail closed.
3. unknown or unavailable module permissions deny rather than allow.
4. registration cannot mutate role grants implicitly or widen principal scope.
5. disable/re-enable preserves safe policy/history semantics while accurately reflecting availability.
6. same-tenant allow and cross-tenant/scope negative tests remain governed by existing P02 authorization behavior.
7. optional capability association cannot grant capability invocation or permission authority.
8. Go quality and all retained P01/P02/P03.01-P03.06 regressions pass on GitHub-hosted `ubuntu-24.04`.
9. the dedicated P03.07 verifier pins declaration, collision, availability, non-granting and authorization-boundary invariants.
10. no P03.08+ or business-feature runtime is introduced.

## Completion evidence

Accepted implementation evidence:

- implementation issue: #110 — completed;
- implementation PR: #111 — merged;
- final exact implementation head: `28e36b3ac3183f28ec500f1e70b1fefe02c0c325`;
- implementation merge: `66f8b4cc630f6cd865e440a62478df365e042a31`;
- canonical Governance run/job: `33195104185 / 98930123416` — **PASS**;
- runner: GitHub-hosted `ubuntu-24.04`, Linux/X64;
- Go: `1.26.7`;
- retained verifier: `scripts/verify_p03_07.sh`;
- evidence: `docs/roadmap/evidence/P03.07_COMPLETION_2026-08-28.md`.

Diagnostic-only runs are retained as their actual state: Governance #474 / `33192567020 / 98921494281` failed repository Go quality and Governance #476 / `33194438411 / 98927852853` failed only the dedicated verifier source-marker assertion before a line-safe correction. Neither is acceptance authority.

## State transition

`planned -> active -> verification -> done` is complete for P03.07. P03.08 remains non-authoritative until the separate P03.07 closure / P03.08 activation carrier itself passes exact-final-head canonical GitHub-hosted governance, merges to protected `main`, and protected-main state is re-read. Do not auto-advance beyond P03.08.