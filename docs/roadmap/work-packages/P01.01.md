# P01.01 — Go Workspace / Build Skeleton

- Phase: `P01`
- State: `done`
- Owner/domain: `kernel.platform`
- Depends on: `P00 done`, `P01 entry gate satisfied`
- Blocks: `P01.02`–`P01.12`
- Change class: `B`

> **Completion state:** P01.01 is complete. `kernel_code_authorized=true` remains bounded to the currently active P01 package, which is now P01.02. `business_feature_code_authorized=false` remains mandatory.

## 1. Objective

Create the minimum reproducible Go workspace/build foundation for Omnexa Kernel without introducing business-domain behavior, persistence, tenancy, module runtime, workflow, product APIs or later P01 capabilities.

P01.01 established structure before behavior: repository-owned Go toolchain/version declaration, deterministic build/test entry points, a minimal kernel process skeleton, build/source metadata and hosted CI evidence from a clean checkout.

## 2. Implemented scope

- repository-owned Go `1.26.7` declaration in `.go-version`;
- root `go.mod` / `go.work` workspace foundation;
- minimal `kernel/cmd/omnexa` process entrypoint;
- `kernel/internal/buildinfo` source/version metadata package and unit tests;
- deterministic `scripts/verify_p01_01.sh` verification wrapper;
- canonical developer-command mapping for P01.01;
- GitHub-hosted `ubuntu-24.04` CI integration;
- Go build/test artifact ignore rules.

## 3. Explicitly not implemented

P01.01 did not implement configuration/environment semantics (`P01.02`), structured application errors (`P01.03`), PostgreSQL/migrations (`P01.04`), cache/storage, telemetry, health endpoints, jobs, feature flags, audit transport, full developer CLI, identity/tenancy, module runtime or business modules.

## 4. Architecture boundary

- Owner: `kernel.platform` / repository build foundation.
- Data/schema owner: none.
- Database migrations: none.
- Public business APIs: none.
- Events/workflows: none.
- External infrastructure dependencies: none.
- Third-party runtime dependencies: none; standard library only.

## 5. Acceptance criteria

1. **AC-01 — PASS**: clean hosted checkout runs the declared P01.01 verification path.
2. **AC-02 — PASS**: workspace/module graph contains only kernel/build-skeleton scope.
3. **AC-03 — PASS**: `go test ./kernel/...` passes.
4. **AC-04 — PASS**: `gofmt`, workspace/module canonicality and `go vet` pass.
5. **AC-05 — PASS**: kernel skeleton builds on GitHub-hosted `ubuntu-24.04`.
6. **AC-06 — PASS**: built process starts/exits deterministically without external infrastructure.
7. **AC-07 — PASS**: build/version metadata identifies source/version without secret/local-path leakage.
8. **AC-08 — PASS**: no database, tenancy/authz, module-runtime or business functionality introduced.
9. **AC-09 — PASS**: applicable `G0`, `G1`, `G2`, `G7` evidence passed.
10. **AC-10 — PASS**: completion evidence is recorded before state advancement.

## 6. Completion evidence

Canonical evidence: `docs/roadmap/evidence/P01.01_COMPLETION_2026-08-22.md`.

```text
Go version: go1.26.7
G0 governance: PASS
G1 format/static: PASS
G2 unit/smoke: PASS
G7 build/package: PASS
Governance workflow run: 32562869345
Governance job: 97007065640
Runner: github-hosted / ubuntu-24.04 / X64
PR head SHA: 2f5e4088ce428bbd0ee45db94f7a282509e73fb6
Merge SHA: 7257977264d788663083fa215462b1828f1e5afb
```

`G3/G4/G5/G6/G8` capabilities not exercised by this package remain `N/A` where appropriate; they are not relabeled PASS.

## 7. State transition

`planned -> active -> verification -> done` is complete.

Only `P01.02 — Configuration & environment system` may now be active. P01.03+ and P02/P03/business-feature implementation remain unauthorized until their governed transitions.
